#ifndef MODBUS_RS_H
#define MODBUS_RS_H

#pragma once

/* Auto-generated by cbindgen - DO NOT EDIT */

/* Active feature configuration defines built into the library */
#define MBUS_FEATURE_C_CLIENT 1
#define MBUS_FEATURE_C_SERVER 1
#define MBUS_FEATURE_C_GATEWAY 1
#define MBUS_FEATURE_COILS 1
#define MBUS_FEATURE_HOLDING_REGISTERS 1
#define MBUS_FEATURE_INPUT_REGISTERS 1
#define MBUS_FEATURE_DISCRETE_INPUTS 1
#define MBUS_FEATURE_FIFO 1
#define MBUS_FEATURE_FILE_RECORD 1
#define MBUS_FEATURE_DIAGNOSTICS 1
#define MBUS_FEATURE_TRAFFIC 1
#define MBUS_FEATURE_SERVER_TRAFFIC 1
#define MBUS_FEATURE_NETWORK_TCP 1
#define MBUS_FEATURE_SERIAL_RTU 1
#define MBUS_FEATURE_SERIAL_ASCII 1
#define MBUS_FEATURE_DOTNET 1
#define MBUS_FEATURE_GO 1


#include <stdint.h>
#include <stdbool.h>
#include <stddef.h>
/* Common forward declarations */
#if defined(MBUS_FEATURE_COILS)
typedef struct MbusCoils MbusCoils;
#endif

#if defined(MBUS_FEATURE_DISCRETE_INPUTS)
typedef struct MbusDiscreteInputs MbusDiscreteInputs;
#endif

#if defined(MBUS_FEATURE_FIFO)
typedef struct MbusFifoQueue MbusFifoQueue;
#endif

#if defined(MBUS_FEATURE_HOLDING_REGISTERS) || defined(MBUS_FEATURE_INPUT_REGISTERS)
typedef struct MbusRegisters MbusRegisters;
#endif

#if defined(MBUS_FEATURE_HOLDING_REGISTERS)
typedef struct MbusHoldingRegisters MbusHoldingRegisters;
#endif

#if defined(MBUS_FEATURE_INPUT_REGISTERS)
typedef struct MbusInputRegisters MbusInputRegisters;
#endif

#if defined(MBUS_FEATURE_DOTNET)
typedef struct MbusDnTcpClient MbusDnTcpClient;
#endif

#if defined(MBUS_FEATURE_GO)
typedef struct MbusGoTcpClient MbusGoTcpClient;
typedef struct MbusGoSerialClient MbusGoSerialClient;
typedef struct MbusGoTcpServer MbusGoTcpServer;
typedef struct MbusGoTcpGateway MbusGoTcpGateway;

/* Go server vtable manually defined because cbindgen doesn't handle conditional fields correctly */
typedef struct MbusGoServerVtable {
    void *ctx;
    int32_t (*read_coils)(void*, uint16_t, uint16_t, uint8_t*, uint16_t*);
    int32_t (*write_single_coil)(void*, uint16_t, uint8_t);
    int32_t (*write_multiple_coils)(void*, uint16_t, const uint8_t*, uint16_t, uint16_t);
    int32_t (*read_discrete_inputs)(void*, uint16_t, uint16_t, uint8_t*, uint16_t*);
    int32_t (*read_holding_registers)(void*, uint16_t, uint16_t, uint16_t*, uint16_t*);
    int32_t (*read_input_registers)(void*, uint16_t, uint16_t, uint16_t*, uint16_t*);
    int32_t (*write_single_register)(void*, uint16_t, uint16_t);
    int32_t (*write_multiple_registers)(void*, uint16_t, const uint8_t*, uint16_t);
    int32_t (*mask_write_register)(void*, uint16_t, uint16_t, uint16_t);
    int32_t (*read_write_multiple_registers)(void*, uint16_t, uint16_t, uint16_t, const uint16_t*, uint16_t*);
    int32_t (*read_fifo_queue)(void*, uint16_t, uint16_t*, uint16_t*);
    int32_t (*read_file_record)(void*, uint16_t, uint16_t, uint16_t, uint16_t*, uint16_t*);
    int32_t (*write_file_record)(void*, uint16_t, uint16_t, uint16_t, const uint16_t*);
    int32_t (*read_exception_status)(void*, uint8_t*);
    int32_t (*diagnostics)(void*, uint16_t, uint16_t, uint16_t*, uint16_t*);
    int32_t (*get_comm_event_counter)(void*, uint16_t*, uint16_t*);
    int32_t (*get_comm_event_log)(void*, uint8_t*, uint16_t*);
    int32_t (*report_server_id)(void*, uint8_t*, uint16_t*);
} MbusGoServerVtable;
#endif


#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_GATEWAY))
/**
 * Maximum number of downstream channels per gateway instance.
 */
#define MAX_DOWNSTREAM_CHANNELS 8
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_GATEWAY))
/**
 * Pipeline / transaction-map size for sync gateway poll cycles (always 1).
 */
#define TXN_SIZE 1
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_GATEWAY))
/**
 * Maximum number of exact-unit routing entries per gateway instance.
 */
#define MAX_UNIT_ROUTES 32
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_GATEWAY))
/**
 * Maximum number of unit-ID-range routing entries per gateway instance.
 */
#define MAX_RANGE_ROUTES 8
#endif

#if (defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32))
/**
 * Maximum byte buffer size passed to vtable read callbacks.
 */
#define VTABLE_BUF_BYTES 256
#endif

#if (defined(MBUS_FEATURE_GO) && !defined(MBUS_TARGET_WASM32))
/**
 * Maximum byte buffer size passed to vtable read callbacks.
 */
#define VTABLE_BUF_BYTES 256
#endif

#if (defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32))
/**
 * Maximum word (u16) buffer size passed to vtable read callbacks.
 */
#define VTABLE_BUF_WORDS 128
#endif

#if (defined(MBUS_FEATURE_GO) && !defined(MBUS_TARGET_WASM32))
/**
 * Maximum word (u16) buffer size passed to vtable read callbacks.
 */
#define VTABLE_BUF_WORDS 128
#endif

#if (defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO))
/**
 * C-compatible status code returned by every `mbus_*` function.
 *
 * `MBUS_OK` (0) means the request was successfully queued. Actual response data
 * is delivered later via the callbacks in `MbusCallbacks`.
 */
typedef enum MbusStatusCode {
#if (defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO))
    /**
     * Success.
     */
    MbusOk = 0,
#endif
#if (defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO))
    /**
     * Frame parse error.
     */
    MbusErrParseError,
#endif
#if (defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO))
    /**
     * Basic parse error.
     */
    MbusErrBasicParseError,
#endif
#if (defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO))
    /**
     * Response timeout.
     */
    MbusErrTimeout,
#endif
#if (defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO))
    /**
     * Modbus exception response.
     */
    MbusErrModbusException,
#endif
#if (defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO))
    /**
     * I/O error.
     */
    MbusErrIoError,
#endif
#if (defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO))
    /**
     * Unexpected error.
     */
    MbusErrUnexpected,
#endif
#if (defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO))
    /**
     * Connection lost.
     */
    MbusErrConnectionLost,
#endif
#if (defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO))
    /**
     * Unsupported function.
     */
    MbusErrUnsupportedFunction,
#endif
#if (defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO))
    /**
     * Reserved sub-function.
     */
    MbusErrReservedSubFunction,
#endif
#if (defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO))
    /**
     * Invalid PDU length.
     */
    MbusErrInvalidPduLength,
#endif
#if (defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO))
    /**
     * Invalid ADU length.
     */
    MbusErrInvalidAduLength,
#endif
#if (defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO))
    /**
     * Connection failed.
     */
    MbusErrConnectionFailed,
#endif
#if (defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO))
    /**
     * Connection closed.
     */
    MbusErrConnectionClosed,
#endif
#if (defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO))
    /**
     * Buffer too small.
     */
    MbusErrBufferTooSmall,
#endif
#if (defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO))
    /**
     * Buffer length mismatch.
     */
    MbusErrBufferLenMismatch,
#endif
#if (defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO))
    /**
     * Send failed.
     */
    MbusErrSendFailed,
#endif
#if (defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO))
    /**
     * Invalid address.
     */
    MbusErrInvalidAddress,
#endif
#if (defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO))
    /**
     * Invalid offset.
     */
    MbusErrInvalidOffset,
#endif
#if (defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO))
    /**
     * Too many in-flight requests.
     */
    MbusErrTooManyRequests,
#endif
#if (defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO))
    /**
     * Invalid function code.
     */
    MbusErrInvalidFunctionCode,
#endif
#if (defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO))
    /**
     * No retries left.
     */
    MbusErrNoRetriesLeft,
#endif
#if (defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO))
    /**
     * Too many file read sub-requests.
     */
    MbusErrTooManyFileReadSubRequests,
#endif
#if (defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO))
    /**
     * File read PDU overflow.
     */
    MbusErrFileReadPduOverflow,
#endif
#if (defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO))
    /**
     * Unexpected response.
     */
    MbusErrUnexpectedResponse,
#endif
#if (defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO))
    /**
     * Invalid transport.
     */
    MbusErrInvalidTransport,
#endif
#if (defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO))
    /**
     * Invalid slave address.
     */
    MbusErrInvalidSlaveAddress,
#endif
#if (defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO))
    /**
     * Checksum error.
     */
    MbusErrChecksumError,
#endif
#if (defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO))
    /**
     * Invalid configuration.
     */
    MbusErrInvalidConfiguration,
#endif
#if (defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO))
    /**
     * Invalid number of expected responses.
     */
    MbusErrInvalidNumOfExpectedRsps,
#endif
#if (defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO))
    /**
     * Invalid data length.
     */
    MbusErrInvalidDataLen,
#endif
#if (defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO))
    /**
     * Invalid quantity.
     */
    MbusErrInvalidQuantity,
#endif
#if (defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO))
    /**
     * Invalid value.
     */
    MbusErrInvalidValue,
#endif
#if (defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO))
    /**
     * Invalid AND mask.
     */
    MbusErrInvalidAndMask,
#endif
#if (defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO))
    /**
     * Invalid OR mask.
     */
    MbusErrInvalidOrMask,
#endif
#if (defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO))
    /**
     * Invalid byte count.
     */
    MbusErrInvalidByteCount,
#endif
#if (defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO))
    /**
     * Invalid device identification.
     */
    MbusErrInvalidDeviceIdentification,
#endif
#if (defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO))
    /**
     * Invalid device ID code.
     */
    MbusErrInvalidDeviceIdCode,
#endif
#if (defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO))
    /**
     * Invalid MEI type.
     */
    MbusErrInvalidMeiType,
#endif
#if (defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO))
    /**
     * Invalid broadcast address.
     */
    MbusErrInvalidBroadcastAddress,
#endif
#if (defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO))
    /**
     * Broadcast not allowed.
     */
    MbusErrBroadcastNotAllowed,
#endif
#if (defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO))
    /**
     * Framing error (protocol timing violation).
     */
    MbusErrFramingError,
#endif
#if (defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO))
    /**
     * Null pointer passed.
     */
    MbusErrNullPointer,
#endif
#if (defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO))
    /**
     * Invalid UTF-8 string.
     */
    MbusErrInvalidUtf8,
#endif
#if (defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO))
    /**
     * Invalid client ID (freed or never allocated).
     */
    MbusErrInvalidClientId,
#endif
#if (defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO))
    /**
     * Client pool is full — no free slots.
     */
    MbusErrPoolFull,
#endif
#if (defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO))
    /**
     * Client type mismatch (e.g. TCP call on serial ID).
     */
    MbusErrClientTypeMismatch,
#endif
#if (defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO))
    /**
     * Client is busy (indicates an illegal re-entrant call from a callback).
     */
    MbusErrBusy,
#endif
} MbusStatusCode;
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT))
/**
 * Serial framing mode.
 */
typedef enum MbusSerialMode {
#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT))
    /**
     * Modbus RTU binary framing with CRC-16.
     */
    MbusSerialRtu = 0,
#endif
#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT))
    /**
     * Modbus ASCII framing with LRC.
     */
    MbusSerialAscii,
#endif
} MbusSerialMode;
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT))
/**
 * Backoff strategy selector for retry logic.
 */
typedef enum MbusBackoffStrategy {
#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT))
    /**
     * Retry immediately with no delay.
     */
    MbusBackoffImmediate = 0,
#endif
#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT))
    /**
     * Retry after a fixed delay (`backoff_base_delay_ms`).
     */
    MbusBackoffFixed,
#endif
#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT))
    /**
     * Retry with exponentially increasing delay, capped at `backoff_max_delay_ms`.
     */
    MbusBackoffExponential,
#endif
#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT))
    /**
     * Retry with linearly increasing delay, capped at `backoff_max_delay_ms`.
     */
    MbusBackoffLinear,
#endif
} MbusBackoffStrategy;
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII))))
/**
 * Exception code returned by every C server callback.
 *
 * Maps 1-to-1 with the Modbus standard exception codes:
 *
 * | Value | Name | When to return |
 * |-------|------|----------------|
 * | `0` | `Ok` | Request handled successfully. The server sends a normal response. |
 * | `1` | `IllegalFunction` | This function code is not supported by your server implementation. |
 * | `2` | `IllegalDataAddress` | The requested `address` or `quantity` is outside the server's valid range. |
 * | `3` | `IllegalDataValue` | A value in the request data field is not acceptable (e.g. out-of-range register value). |
 * | `4` | `ServerDeviceFailure` | An unrecoverable error occurred while processing the request (hardware fault, internal error). |
 *
 * # Notes
 * - `Ok = 0` is a **non-standard sentinel** used only by this C API.  It is
 *   never sent on the wire; the server translates it to a normal success response.
 * - For **write** function codes (FC05, FC06, FC0F, FC10, FC15, FC16, FC17),
 *   `Ok` means the write was accepted and committed.  The server echoes the
 *   request back to the master as specified by the Modbus protocol.
 * - For **read** function codes (FC01–FC04, FC07, FC0B, FC0C, FC11, FC14, FC17,
 *   FC18, FC2B), `Ok` means the `out_*` fields have been populated with valid
 *   response data.
 * - If you detect a timing violation in application logic (e.g. a sensor timed
 *   out), return `ServerDeviceFailure`; do **not** return `FramingError` — that
 *   is a transport-level status only returned by the `recv` callback.
 */
typedef enum MbusServerExceptionCode {
#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII))))
    /**
     * No exception — the callback handled the request successfully.
     */
    Ok = 0,
#endif
#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII))))
    /**
     * FC is not supported / not implemented by this server.
     */
    IllegalFunction = 1,
#endif
#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII))))
    /**
     * Address or offset is outside the server's valid range.
     */
    IllegalDataAddress = 2,
#endif
#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII))))
    /**
     * A data value in the request is not acceptable.
     */
    IllegalDataValue = 3,
#endif
#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII))))
    /**
     * The server could not complete the request due to an internal failure.
     */
    ServerDeviceFailure = 4,
#endif
} MbusServerExceptionCode;
#endif

#if (defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32))
/**
 * Opaque handle to an asynchronous Modbus serial client (RTU or ASCII).
 */
typedef struct MbusDnSerialClient MbusDnSerialClient;
#endif

#if (defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32))
/**
 * Opaque handle to an asynchronous Modbus TCP client.
 *
 * Created by [`mbus_dn_tcp_client_new`] and destroyed by
 * [`mbus_dn_tcp_client_free`].  Always passed by raw pointer over FFI.
 *
 * The struct itself is heap-allocated (`Box::into_raw`); `Arc` lets the
 * shared Tokio runtime hold a clone for the lifetime of any in-flight
 * request without preventing destruction once the C# `SafeHandle` runs
 * `_free`.
 */
typedef struct MbusDnTcpClient MbusDnTcpClient;
#endif

#if (defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32))
/**
 * Opaque gateway handle.
 */
typedef struct MbusDnTcpGateway MbusDnTcpGateway;
#endif

#if (defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32))
/**
 * Opaque handle to an async Modbus TCP server.
 */
typedef struct MbusDnTcpServer MbusDnTcpServer;
#endif

#if (defined(MBUS_FEATURE_GO) && !defined(MBUS_TARGET_WASM32))
/**
 * Opaque handle to an asynchronous Modbus serial client (RTU or ASCII).
 */
typedef struct MbusGoSerialClient MbusGoSerialClient;
#endif

#if (defined(MBUS_FEATURE_GO) && !defined(MBUS_TARGET_WASM32))
/**
 * Opaque handle to an asynchronous Modbus TCP client.
 *
 * Created by [`mbus_go_tcp_client_new`] and destroyed by
 * [`mbus_go_tcp_client_free`].  Always passed by raw pointer over FFI.
 *
 * The struct itself is heap-allocated (`Box::into_raw`); `Arc` lets the
 * shared Tokio runtime hold a clone for the lifetime of any in-flight
 * request without preventing destruction once the Go finalizer runs
 * `_free`.
 */
typedef struct MbusGoTcpClient MbusGoTcpClient;
#endif

#if (defined(MBUS_FEATURE_GO) && !defined(MBUS_TARGET_WASM32))
/**
 * Opaque gateway handle.
 */
typedef struct MbusGoTcpGateway MbusGoTcpGateway;
#endif

#if (defined(MBUS_FEATURE_GO) && !defined(MBUS_TARGET_WASM32))
/**
 * Opaque handle to an async Modbus TCP server.
 */
typedef struct MbusGoTcpServer MbusGoTcpServer;
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT))
/**
 * Client ID type: an opaque `u16` index into one of the three sub-pools.
 * Use `MBUS_INVALID_CLIENT_ID` (0xFFFF) as the sentinel "no client" value.
 */
typedef uint16_t MbusClientId;
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT))
/**
 * Configuration for a Modbus Serial (RTU or ASCII) client.
 *
 * `port_name` must remain valid for the duration of `mbus_serial_client_new`.
 */
typedef struct MbusSerialConfig {
    /**
     * Null-terminated serial port path (e.g. `"/dev/ttyUSB0"` or `"COM3"`).
     */
    const char *port_name;
    /**
     * Baud rate (e.g. 9600, 19200, 115200).
     */
    uint32_t baud_rate;
    /**
     * Framing mode: RTU or ASCII.
     */
    enum MbusSerialMode mode;
    /**
     * Timeout waiting for a Modbus response, in milliseconds.
     */
    uint32_t response_timeout_ms;
    /**
     * Number of retry attempts.
     */
    uint8_t retries;
    /**
     * Backoff strategy between retries.
     */
    enum MbusBackoffStrategy backoff_strategy;
    /**
     * Base delay (ms).
     */
    uint32_t backoff_base_delay_ms;
    /**
     * Maximum delay cap (ms).
     */
    uint32_t backoff_max_delay_ms;
    /**
     * Jitter percentage (0–100).
     */
    uint8_t jitter_percent;
} MbusSerialConfig;
#endif

#if (defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO))
/**
 * C-provided transport callbacks — the only interface between C code and the Rust Modbus stack.
 *
 * Fill in this struct and pass it to a `mbus_*_new` constructor.  The Rust stack
 * calls these function pointers whenever it needs to open, close, send data, or
 * receive data over the underlying physical medium (TCP socket, UART, etc.).
 *
 * # Requirements
 *
 * - **All five callbacks must be non-`NULL`.**  `validate_transport_callbacks` checks
 *   this; constructors reject a struct with any `NULL` slot.
 * - Every callback receives the same `userdata` pointer that is stored in this struct.
 *   Use it to pass your driver state (file descriptor, hardware register base, etc.).
 * - Callbacks may be called from any context that calls `mbus_*_poll`, `mbus_*_connect`,
 *   or `mbus_*_send`.  If you use locking in your driver, acquire and release the lock
 *   inside the callback — do **not** hold an external lock while polling.
 * - Callbacks must **never** call back into the Modbus stack (no `mbus_*` functions
 *   from inside a callback) — doing so causes re-entrant undefined behaviour.
 *
 * # Lifetime
 *
 * The struct (and everything `userdata` points to) must remain valid until
 * `mbus_*_free` is called on every server or client that uses it.
 *
 * # Minimal example (C, TCP)
 *
 * ```c
 * static MbusStatusCode my_connect(void *ud) {
 *     MyCtx *ctx = (MyCtx *)ud;
 *     ctx->fd = socket_open(ctx->host, ctx->port);
 *     return ctx->fd >= 0 ? MBUS_OK : MBUS_ERR_CONNECTION_FAILED;
 * }
 * static MbusStatusCode my_disconnect(void *ud) {
 *     socket_close(((MyCtx *)ud)->fd);
 *     return MBUS_OK;
 * }
 * static MbusStatusCode my_send(const uint8_t *data, uint16_t len, void *ud) {
 *     return socket_write(((MyCtx *)ud)->fd, data, len) == len
 *         ? MBUS_OK : MBUS_ERR_SEND_FAILED;
 * }
 * static MbusStatusCode my_recv(uint8_t *buf, uint16_t cap,
 *                               uint16_t *out_len, void *ud) {
 *     int n = socket_read_nonblocking(((MyCtx *)ud)->fd, buf, cap);
 *     if (n > 0) { *out_len = (uint16_t)n; return MBUS_OK; }
 *     return n == 0 ? MBUS_ERR_TIMEOUT : MBUS_ERR_IO_ERROR;
 * }
 * static uint8_t my_is_connected(void *ud) {
 *     return ((MyCtx *)ud)->fd >= 0 ? 1 : 0;
 * }
 *
 * static MbusTransportCallbacks g_transport = {
 *     .userdata       = &g_ctx,
 *     .on_connect     = my_connect,
 *     .on_disconnect  = my_disconnect,
 *     .on_send        = my_send,
 *     .on_recv        = my_recv,
 *     .on_is_connected = my_is_connected,
 * };
 * ```
 */
typedef struct MbusTransportCallbacks {
    /**
     * Opaque pointer passed unchanged to every callback invocation.
     *
     * Typically points to your driver context struct (file descriptor, register
     * base address, mutex handle, etc.).  May be `NULL` if your callbacks do not
     * need per-instance state (e.g. a singleton UART driver).
     */
    void *userdata;
    /**
     * Open the physical connection.
     *
     * Called once by `mbus_*_connect`.  The stack does not call `on_send` or
     * `on_recv` until this returns `MBUS_OK`.
     *
     * # What to do
     * - Open the socket / serial port / SPI bus.
     * - Perform any hardware initialisation (baud rate, parity, stop bits).
     * - For serial servers: configure the UART but do **not** start receiving
     *   until you return — the stack will call `on_recv` on the first poll.
     *
     * # Return values
     * | Code | Meaning |
     * |------|---------|
     * | `MBUS_OK` | Connection established; stack proceeds normally. |
     * | `MBUS_ERR_CONNECTION_FAILED` | Could not open the port/socket (e.g. ENOENT, EACCES). |
     * | `MBUS_ERR_IO_ERROR` | Low-level I/O failure during hardware setup. |
     * | `MBUS_ERR_INVALID_CONFIGURATION` | Supplied config is not supported by this hardware. |
     *
     * Any other non-`MBUS_OK` code is mapped to `MbusError::Unexpected` internally.
     */
    enum MbusStatusCode (*on_connect)(void *userdata);
    /**
     * Close the physical connection.
     *
     * Called by `mbus_*_disconnect` and during server teardown.  After this
     * returns, the stack will not call `on_send` or `on_recv` until
     * `on_connect` is called again.
     *
     * # What to do
     * - Release the socket / serial port file descriptor.
     * - For RS-485: de-assert the transmit-enable line if your driver holds it.
     * - This function should be idempotent — calling it on an already-closed
     *   transport must not crash or return an error.
     *
     * # Return values
     * | Code | Meaning |
     * |------|---------|
     * | `MBUS_OK` | Disconnected successfully (or was already disconnected). |
     * | `MBUS_ERR_IO_ERROR` | An error occurred while flushing or closing the port. |
     *
     * The return value is checked but not acted upon by the stack beyond logging.
     */
    enum MbusStatusCode (*on_disconnect)(void *userdata);
    /**
     * Write a complete Modbus ADU frame to the medium.
     *
     * The stack calls this once per outgoing frame with a fully-formed byte
     * slice (`data[0..len]`).  The function must write **all** `len` bytes
     * atomically from the stack's perspective — partial writes are not retried
     * at the byte level.
     *
     * # Parameters
     * - `data`     — Pointer to the frame bytes.  Valid only for the duration
     *   of this call; do not retain it.
     * - `len`      — Number of bytes to send (always ≤ `MAX_ADU_FRAME_LEN` = 256).
     * - `userdata` — The value stored in `MbusTransportCallbacks::userdata`.
     *
     * # RS-485 / half-duplex requirement
     * Assert the TX-enable line **before** writing the first byte and
     * de-assert it **after** the last byte has been shifted out of the UART
     * FIFO (not just after the write syscall returns).  Failing to do this
     * corrupts the frame from the perspective of other nodes on the bus.
     *
     * # Return values
     * | Code | Meaning |
     * |------|---------|
     * | `MBUS_OK` | All bytes written successfully. |
     * | `MBUS_ERR_SEND_FAILED` | Write failed partway through (broken pipe, UART overflow). |
     * | `MBUS_ERR_IO_ERROR` | Hardware-level I/O error (DMA fault, framing error on TX). |
     * | `MBUS_ERR_CONNECTION_CLOSED` | The remote end closed the connection (TCP only). |
     *
     * On failure the stack queues the frame for retry (up to `max_send_retries`).
     */
    enum MbusStatusCode (*on_send)(const uint8_t *data, uint16_t len, void *userdata);
    /**
     * Read the next available Modbus ADU frame from the medium.
     *
     * The stack calls this once per `mbus_*_poll` invocation.  The function
     * must be **non-blocking** (or use a very short read timeout) so that the
     * poll loop remains responsive.
     *
     * # Parameters
     * - `buffer`     — Rust-allocated scratch buffer; write received bytes here.
     *   Valid only for the duration of this call.
     * - `buffer_cap` — Capacity of `buffer` in bytes (always `MAX_ADU_FRAME_LEN` = 256).
     *   Never write more than `buffer_cap` bytes.
     * - `out_len`    — Write the number of bytes placed in `buffer` here.
     *   Must be set to `0` when returning anything other than `MBUS_OK`.
     * - `userdata`   — The value stored in `MbusTransportCallbacks::userdata`.
     *
     * # TCP / stream transports
     * Read as many bytes as are available right now (non-blocking).  The Rust
     * layer uses a sliding window to assemble complete frames from partial reads;
     * you do not need to wait for a full frame.
     *
     * # Serial RTU transports — timing requirements
     * The `on_recv` callback is the correct place to enforce Modbus
     * inter-character timing (§2.5 of the Modbus Serial Line Specification):
     *
     * - **t3.5 (end-of-frame):** Wait until the line has been silent for at
     *   least 3.5 character times after the last received byte.  Only then
     *   return the accumulated bytes with `MBUS_OK`.  This ensures each
     *   `on_recv` call delivers exactly one complete frame.
     *
     * - **t1.5 (framing violation):** If silence of ≥ 1.5 character times is
     *   detected *between* bytes of the same frame, the frame is corrupt.
     *   Return **`MBUS_ERR_FRAMING_ERROR`** immediately.  The stack will:
     *     1. Discard all bytes buffered so far.
     *     2. Keep the transport connected.
     *     3. Resume waiting for a clean frame on the next poll.
     *
     * # Return values
     * | Code | Meaning |
     * |------|---------|
     * | `MBUS_OK` | Frame bytes written into `buffer`; `*out_len > 0`. |
     * | `MBUS_ERR_TIMEOUT` | No data available this poll (bus idle / no client request yet). The stack skips processing and polls again. |
     * | `MBUS_ERR_FRAMING_ERROR` | **Serial only.** t1.5 inter-character gap detected mid-frame. Stack discards the partial frame and continues. |
     * | `MBUS_ERR_IO_ERROR` | Hardware read error (UART overrun, DMA fault). Stack marks the transport as disconnected. |
     * | `MBUS_ERR_CONNECTION_CLOSED` | Remote closed the connection (TCP) or the port was unexpectedly closed. Stack disconnects and stops polling. |
     * | `MBUS_ERR_CONNECTION_LOST` | Connection dropped unexpectedly (e.g. TCP RST). Same effect as `CONNECTION_CLOSED`. |
     * | `MBUS_ERR_BUFFER_TOO_SMALL` | Frame exceeded `buffer_cap`. Stack discards data (should not happen under normal conditions). |
     *
     * **Do not** set `*out_len` to a non-zero value when returning an error code —
     * the stack ignores `buffer` contents on any non-`MBUS_OK` return.
     */
    enum MbusStatusCode (*on_recv)(uint8_t *buffer,
                                   uint16_t buffer_cap,
                                   uint16_t *out_len,
                                   void *userdata);
    /**
     * Query whether the transport is currently open/connected.
     *
     * Called by `mbus_*_is_connected` and internally after a recv/send error
     * to decide whether to attempt a reconnect.
     *
     * # What to do
     * - Return `1` (non-zero) if the port or socket is open and ready for I/O.
     * - Return `0` if the transport has been closed, has not yet been opened,
     *   or is in an error state.
     * - This function must be **side-effect-free** and **cheap** — it may be
     *   called multiple times per poll cycle.
     *
     * # Return values
     * | Value | Meaning |
     * |-------|---------|
     * | `1` (or any non-zero) | Transport is open and ready. |
     * | `0` | Transport is closed or in error. |
     */
    uint8_t (*on_is_connected)(void *userdata);
} MbusTransportCallbacks;
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT)) && defined(MBUS_FEATURE_COILS))
/**
 * Context passed to the read-coils callback.
 */
typedef struct MbusReadCoilsCtx {
    /**
     * Transaction ID.
     */
    uint16_t txn_id;
    /**
     * Unit / slave ID.
     */
    uint8_t unit_id;
    /**
     * Opaque pointer to the coils data (valid during this callback only).
     */
    const MbusCoils *coils;
    /**
     * User-provided opaque pointer.
     */
    void *userdata;
} MbusReadCoilsCtx;
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT)) && defined(MBUS_FEATURE_COILS))
/**
 * Context passed to the write-single-coil callback.
 */
typedef struct MbusWriteSingleCoilCtx {
    /**
     * Transaction ID.
     */
    uint16_t txn_id;
    /**
     * Unit / slave ID.
     */
    uint8_t unit_id;
    /**
     * Coil address.
     */
    uint16_t address;
    /**
     * Written value (1 = ON, 0 = OFF).
     */
    uint8_t value;
    /**
     * User-provided opaque pointer.
     */
    void *userdata;
} MbusWriteSingleCoilCtx;
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT)) && defined(MBUS_FEATURE_COILS))
/**
 * Context passed to the write-multiple-coils callback.
 */
typedef struct MbusWriteMultipleCoilsCtx {
    /**
     * Transaction ID.
     */
    uint16_t txn_id;
    /**
     * Unit / slave ID.
     */
    uint8_t unit_id;
    /**
     * Starting coil address.
     */
    uint16_t address;
    /**
     * Number of coils written.
     */
    uint16_t quantity;
    /**
     * User-provided opaque pointer.
     */
    void *userdata;
} MbusWriteMultipleCoilsCtx;
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT)) && defined(MBUS_FEATURE_HOLDING_REGISTERS))
/**
 * Context passed to the read-holding-registers callback.
 */
typedef struct MbusReadHoldingRegistersCtx {
    /**
     * Transaction ID.
     */
    uint16_t txn_id;
    /**
     * Unit / slave ID.
     */
    uint8_t unit_id;
    /**
     * Opaque pointer to registers data.
     */
    const MbusHoldingRegisters *registers;
    /**
     * User-provided opaque pointer.
     */
    void *userdata;
} MbusReadHoldingRegistersCtx;
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT)) && defined(MBUS_FEATURE_INPUT_REGISTERS))
/**
 * Context passed to the read-input-registers callback.
 */
typedef struct MbusReadInputRegistersCtx {
    /**
     * Transaction ID.
     */
    uint16_t txn_id;
    /**
     * Unit / slave ID.
     */
    uint8_t unit_id;
    /**
     * Opaque pointer to registers data.
     */
    const MbusInputRegisters *registers;
    /**
     * User-provided opaque pointer.
     */
    void *userdata;
} MbusReadInputRegistersCtx;
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT)) && defined(MBUS_FEATURE_HOLDING_REGISTERS))
/**
 * Context passed to the read-write-multiple-registers callback.
 */
typedef struct MbusReadWriteMultipleRegistersCtx {
    /**
     * Transaction ID.
     */
    uint16_t txn_id;
    /**
     * Unit / slave ID.
     */
    uint8_t unit_id;
    /**
     * Opaque pointer to registers data.
     */
    const MbusHoldingRegisters *registers;
    /**
     * User-provided opaque pointer.
     */
    void *userdata;
} MbusReadWriteMultipleRegistersCtx;
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT)) && defined(MBUS_FEATURE_HOLDING_REGISTERS))
/**
 * Context passed to the write-single-register callback.
 */
typedef struct MbusWriteSingleRegisterCtx {
    /**
     * Transaction ID.
     */
    uint16_t txn_id;
    /**
     * Unit / slave ID.
     */
    uint8_t unit_id;
    /**
     * Register address.
     */
    uint16_t address;
    /**
     * Written value.
     */
    uint16_t value;
    /**
     * User-provided opaque pointer.
     */
    void *userdata;
} MbusWriteSingleRegisterCtx;
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT)) && defined(MBUS_FEATURE_HOLDING_REGISTERS))
/**
 * Context passed to the write-multiple-registers callback.
 */
typedef struct MbusWriteMultipleRegistersCtx {
    /**
     * Transaction ID.
     */
    uint16_t txn_id;
    /**
     * Unit / slave ID.
     */
    uint8_t unit_id;
    /**
     * Starting register address.
     */
    uint16_t address;
    /**
     * Number of registers written.
     */
    uint16_t quantity;
    /**
     * User-provided opaque pointer.
     */
    void *userdata;
} MbusWriteMultipleRegistersCtx;
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT)) && defined(MBUS_FEATURE_HOLDING_REGISTERS))
/**
 * Context passed to the mask-write-register callback.
 */
typedef struct MbusMaskWriteRegisterCtx {
    /**
     * Transaction ID.
     */
    uint16_t txn_id;
    /**
     * Unit / slave ID.
     */
    uint8_t unit_id;
    /**
     * User-provided opaque pointer.
     */
    void *userdata;
} MbusMaskWriteRegisterCtx;
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT)) && defined(MBUS_FEATURE_DISCRETE_INPUTS))
/**
 * Context passed to the read-discrete-inputs callback.
 */
typedef struct MbusReadDiscreteInputsCtx {
    /**
     * Transaction ID.
     */
    uint16_t txn_id;
    /**
     * Unit / slave ID.
     */
    uint8_t unit_id;
    /**
     * Opaque pointer to discrete inputs data.
     */
    const MbusDiscreteInputs *discrete_inputs;
    /**
     * User-provided opaque pointer.
     */
    void *userdata;
} MbusReadDiscreteInputsCtx;
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT)) && defined(MBUS_FEATURE_FIFO))
/**
 * Context passed to the read-fifo-queue callback.
 */
typedef struct MbusReadFifoQueueCtx {
    /**
     * Transaction ID.
     */
    uint16_t txn_id;
    /**
     * Unit / slave ID.
     */
    uint8_t unit_id;
    /**
     * Opaque pointer to FIFO queue data.
     */
    const MbusFifoQueue *fifo_queue;
    /**
     * User-provided opaque pointer.
     */
    void *userdata;
} MbusReadFifoQueueCtx;
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT)) && defined(MBUS_FEATURE_FILE_RECORD))
/**
 * A single sub-request result returned in the `on_read_file_record` callback.
 *
 * `data` points into Rust-owned memory and is **valid only during the callback
 * invocation**. Copy the slice if you need to retain it.
 */
typedef struct MbusFileRecordResult {
    /**
     * Record number for this sub-request.
     */
    uint16_t record_number;
    /**
     * Pointer to the register data for this sub-request chunk.
     */
    const uint16_t *data;
    /**
     * Number of u16 words in `data`.
     */
    uint16_t data_len;
} MbusFileRecordResult;
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT)) && defined(MBUS_FEATURE_FILE_RECORD))
/**
 * Context passed to the read-file-record callback.
 */
typedef struct MbusReadFileRecordCtx {
    /**
     * Transaction ID.
     */
    uint16_t txn_id;
    /**
     * Unit / slave ID.
     */
    uint8_t unit_id;
    /**
     * Pointer to array of sub-request results.
     */
    const struct MbusFileRecordResult *results;
    /**
     * Number of sub-request entries in `results`.
     */
    uint16_t count;
    /**
     * User-provided opaque pointer.
     */
    void *userdata;
} MbusReadFileRecordCtx;
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT)) && defined(MBUS_FEATURE_FILE_RECORD))
/**
 * Context passed to the write-file-record callback.
 */
typedef struct MbusWriteFileRecordCtx {
    /**
     * Transaction ID.
     */
    uint16_t txn_id;
    /**
     * Unit / slave ID.
     */
    uint8_t unit_id;
    /**
     * User-provided opaque pointer.
     */
    void *userdata;
} MbusWriteFileRecordCtx;
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT)) && defined(MBUS_FEATURE_DIAGNOSTICS))
/**
 * Context passed to the read-exception-status callback.
 */
typedef struct MbusReadExceptionStatusCtx {
    /**
     * Transaction ID.
     */
    uint16_t txn_id;
    /**
     * Unit / slave ID.
     */
    uint8_t unit_id;
    /**
     * Exception status byte.
     */
    uint8_t status;
    /**
     * User-provided opaque pointer.
     */
    void *userdata;
} MbusReadExceptionStatusCtx;
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT)) && defined(MBUS_FEATURE_DIAGNOSTICS))
/**
 * Context passed to the diagnostics callback.
 */
typedef struct MbusDiagnosticsCtx {
    /**
     * Transaction ID.
     */
    uint16_t txn_id;
    /**
     * Unit / slave ID.
     */
    uint8_t unit_id;
    /**
     * Diagnostic sub-function code.
     */
    uint16_t sub_fn;
    /**
     * Pointer to diagnostic data words.
     */
    const uint16_t *data;
    /**
     * Number of u16 words in `data`.
     */
    uint16_t data_len;
    /**
     * User-provided opaque pointer.
     */
    void *userdata;
} MbusDiagnosticsCtx;
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT)) && defined(MBUS_FEATURE_DIAGNOSTICS))
/**
 * Context passed to the comm-event-counter callback.
 */
typedef struct MbusCommEventCounterCtx {
    /**
     * Transaction ID.
     */
    uint16_t txn_id;
    /**
     * Unit / slave ID.
     */
    uint8_t unit_id;
    /**
     * Status word.
     */
    uint16_t status;
    /**
     * Event count.
     */
    uint16_t event_count;
    /**
     * User-provided opaque pointer.
     */
    void *userdata;
} MbusCommEventCounterCtx;
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT)) && defined(MBUS_FEATURE_DIAGNOSTICS))
/**
 * Context passed to the comm-event-log callback.
 */
typedef struct MbusCommEventLogCtx {
    /**
     * Transaction ID.
     */
    uint16_t txn_id;
    /**
     * Unit / slave ID.
     */
    uint8_t unit_id;
    /**
     * Status word.
     */
    uint16_t status;
    /**
     * Event count.
     */
    uint16_t event_count;
    /**
     * Message count.
     */
    uint16_t message_count;
    /**
     * Pointer to event bytes.
     */
    const uint8_t *events;
    /**
     * Number of bytes in `events`.
     */
    uint16_t events_len;
    /**
     * User-provided opaque pointer.
     */
    void *userdata;
} MbusCommEventLogCtx;
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT)) && defined(MBUS_FEATURE_DIAGNOSTICS))
/**
 * Context passed to the report-server-id callback.
 */
typedef struct MbusReportServerIdCtx {
    /**
     * Transaction ID.
     */
    uint16_t txn_id;
    /**
     * Unit / slave ID.
     */
    uint8_t unit_id;
    /**
     * Server ID byte.
     */
    uint8_t server_id;
    /**
     * Run indicator (0xFF = ON, 0x00 = OFF).
     */
    uint8_t run_indicator;
    /**
     * Pointer to additional device identifier data.
     */
    const uint8_t *device_identifier;
    /**
     * Length of `device_identifier` data.
     */
    uint16_t identifier_len;
    /**
     * User-provided opaque pointer.
     */
    void *userdata;
} MbusReportServerIdCtx;
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT)) && defined(MBUS_FEATURE_DIAGNOSTICS))
/**
 * Context passed to the read-device-id callback.
 */
typedef struct MbusReadDeviceIdCtx {
    /**
     * Transaction ID.
     */
    uint16_t txn_id;
    /**
     * Unit / slave ID.
     */
    uint8_t unit_id;
    /**
     * Read Device ID code that was requested.
     */
    uint8_t read_device_id_code;
    /**
     * Conformity level reported by the device.
     */
    uint8_t conformity_level;
    /**
     * 1 if more objects follow, 0 if this is the last.
     */
    uint8_t more_follows;
    /**
     * Pointer to raw MEI response bytes.
     */
    const uint8_t *objects;
    /**
     * Number of bytes in `objects`.
     */
    uint16_t objects_len;
    /**
     * User-provided opaque pointer.
     */
    void *userdata;
} MbusReadDeviceIdCtx;
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT))
/**
 * Context passed to the request-failed callback.
 */
typedef struct MbusRequestFailedCtx {
    /**
     * Transaction ID of the failed request.
     */
    uint16_t txn_id;
    /**
     * Unit / slave ID the request was targeting.
     */
    uint8_t unit_id;
    /**
     * Error code describing the failure.
     */
    enum MbusStatusCode error;
    /**
     * User-provided opaque pointer.
     */
    void *userdata;
} MbusRequestFailedCtx;
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT))
/**
 * Table of C function-pointer callbacks.
 *
 * Set a field to `NULL` to ignore that response type. All non-null callbacks
 * will be invoked synchronously from within `mbus_tcp_poll()` /
 * `mbus_serial_poll()`.
 *
 * `userdata` is passed verbatim to every callback invocation; use it to
 * thread your own application context through.
 */
typedef struct MbusCallbacks {
    /**
     * Opaque application context pointer threaded through every callback.
     */
    void *userdata;
    /**
     * Required callback used for all timekeeping in the client state machine.
     *
     * If this callback is NULL, client construction fails.
     */
    uint64_t (*on_current_millis)(void *userdata);
#if defined(MBUS_FEATURE_COILS)
    /**
     * Called when a Read Coils response is received.
     */
    void (*on_read_coils)(const struct MbusReadCoilsCtx *ctx)
#endif
    ;
#if defined(MBUS_FEATURE_COILS)
    /**
     * Called when a Write Single Coil response is received.
     */
    void (*on_write_single_coil)(const struct MbusWriteSingleCoilCtx *ctx)
#endif
    ;
#if defined(MBUS_FEATURE_COILS)
    /**
     * Called when a Write Multiple Coils response is received.
     */
    void (*on_write_multiple_coils)(const struct MbusWriteMultipleCoilsCtx *ctx)
#endif
    ;
#if defined(MBUS_FEATURE_HOLDING_REGISTERS)
    /**
     * Called when a Read Holding Registers response is received.
     */
    void (*on_read_holding_registers)(const struct MbusReadHoldingRegistersCtx *ctx)
#endif
    ;
#if defined(MBUS_FEATURE_INPUT_REGISTERS)
    /**
     * Called when a Read Input Registers response is received.
     */
    void (*on_read_input_registers)(const struct MbusReadInputRegistersCtx *ctx)
#endif
    ;
#if defined(MBUS_FEATURE_HOLDING_REGISTERS)
    /**
     * Called when a Read/Write Multiple Registers response is received.
     */
    void (*on_read_write_multiple_registers)(const struct MbusReadWriteMultipleRegistersCtx *ctx)
#endif
    ;
#if defined(MBUS_FEATURE_HOLDING_REGISTERS)
    /**
     * Called when a Write Single Register response is received.
     */
    void (*on_write_single_register)(const struct MbusWriteSingleRegisterCtx *ctx)
#endif
    ;
#if defined(MBUS_FEATURE_HOLDING_REGISTERS)
    /**
     * Called when a Write Multiple Registers response is received.
     */
    void (*on_write_multiple_registers)(const struct MbusWriteMultipleRegistersCtx *ctx)
#endif
    ;
#if defined(MBUS_FEATURE_HOLDING_REGISTERS)
    /**
     * Called when a Mask Write Register response is received.
     */
    void (*on_mask_write_register)(const struct MbusMaskWriteRegisterCtx *ctx)
#endif
    ;
#if defined(MBUS_FEATURE_DISCRETE_INPUTS)
    /**
     * Called when a Read Discrete Inputs response is received.
     */
    void (*on_read_discrete_inputs)(const struct MbusReadDiscreteInputsCtx *ctx)
#endif
    ;
#if defined(MBUS_FEATURE_FIFO)
    /**
     * Called when a Read FIFO Queue response is received.
     */
    void (*on_read_fifo_queue)(const struct MbusReadFifoQueueCtx *ctx)
#endif
    ;
#if defined(MBUS_FEATURE_FILE_RECORD)
    /**
     * Called when a Read File Record response is received.
     */
    void (*on_read_file_record)(const struct MbusReadFileRecordCtx *ctx)
#endif
    ;
#if defined(MBUS_FEATURE_FILE_RECORD)
    /**
     * Called when a Write File Record response is received.
     */
    void (*on_write_file_record)(const struct MbusWriteFileRecordCtx *ctx)
#endif
    ;
#if defined(MBUS_FEATURE_DIAGNOSTICS)
    /**
     * Called when a Read Exception Status response is received.
     */
    void (*on_read_exception_status)(const struct MbusReadExceptionStatusCtx *ctx)
#endif
    ;
#if defined(MBUS_FEATURE_DIAGNOSTICS)
    /**
     * Called when a Diagnostics response is received.
     */
    void (*on_diagnostics)(const struct MbusDiagnosticsCtx *ctx)
#endif
    ;
#if defined(MBUS_FEATURE_DIAGNOSTICS)
    /**
     * Called when a Comm Event Counter response is received.
     */
    void (*on_comm_event_counter)(const struct MbusCommEventCounterCtx *ctx)
#endif
    ;
#if defined(MBUS_FEATURE_DIAGNOSTICS)
    /**
     * Called when a Comm Event Log response is received.
     */
    void (*on_comm_event_log)(const struct MbusCommEventLogCtx *ctx)
#endif
    ;
#if defined(MBUS_FEATURE_DIAGNOSTICS)
    /**
     * Called when a Report Server ID response is received.
     */
    void (*on_report_server_id)(const struct MbusReportServerIdCtx *ctx)
#endif
    ;
#if defined(MBUS_FEATURE_DIAGNOSTICS)
    /**
     * Called when a Read Device Identification response is received.
     */
    void (*on_read_device_id)(const struct MbusReadDeviceIdCtx *ctx)
#endif
    ;
    /**
     * Called when a request fails (timeout, exception, etc.).
     */
    void (*on_request_failed)(const struct MbusRequestFailedCtx *ctx);
} MbusCallbacks;
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT))
/**
 * Configuration for a Modbus TCP client.
 *
 * All pointer fields (`host`) must remain valid for the duration of the
 * `mbus_tcp_client_new` call. They are copied internally and do not need to
 * outlive the call.
 */
typedef struct MbusTcpConfig {
    /**
     * Null-terminated hostname or IPv4/IPv6 address string (max 63 bytes excl. NUL).
     */
    const char *host;
    /**
     * TCP port (default Modbus port is 502).
     */
    uint16_t port;
    /**
     * Timeout waiting for the TCP connection to be established, in milliseconds.
     */
    uint32_t connection_timeout_ms;
    /**
     * Timeout waiting for a Modbus response, in milliseconds.
     */
    uint32_t response_timeout_ms;
    /**
     * Number of retry attempts before reporting failure via the error callback.
     */
    uint8_t retries;
    /**
     * Backoff strategy between retries.
     */
    enum MbusBackoffStrategy backoff_strategy;
    /**
     * Base delay (ms) used by `MbusBackoffFixed`, `MbusBackoffExponential`, and
     * `MbusBackoffLinear`.
     */
    uint32_t backoff_base_delay_ms;
    /**
     * Maximum delay cap (ms) used by `MbusBackoffExponential` and `MbusBackoffLinear`.
     */
    uint32_t backoff_max_delay_ms;
    /**
     * Jitter percentage (0 = no jitter, 1–100 = ±N% random spread on top of backoff).
     */
    uint8_t jitter_percent;
} MbusTcpConfig;
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT) && defined(MBUS_FEATURE_FILE_RECORD))
/**
 * A single sub-request passed to [`mbus_tcp_read_file_record`] /
 * [`mbus_tcp_write_file_record`] (and their serial equivalents).
 *
 * For **read** operations: populate `file_number`, `record_number`, and
 * `data_len` (number of registers to read); set `data` to NULL.
 *
 * For **write** operations: populate all four fields; `data` must point to
 * at least `data_len` valid `u16` words.
 */
typedef struct MbusSubRequest {
    /**
     * File number (1–65535).
     */
    uint16_t file_number;
    /**
     * Starting record number within the file.
     */
    uint16_t record_number;
    /**
     * Number of 16-bit registers (length). For reads: how many to read.
     * For writes: must equal `data_len`.
     */
    uint16_t record_length;
    /**
     * Pointer to write data (NULL for reads). Valid only during the call.
     */
    const uint16_t *data;
    /**
     * Number of valid words pointed to by `data` (0 for reads).
     */
    uint16_t data_len;
} MbusSubRequest;
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII))))
/**
 * Server ID type: an opaque `u16` index into one of the server sub-pools.
 */
typedef uint16_t MbusServerId;
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII)))) && defined(MBUS_FEATURE_COILS))
/**
 * Request context for FC 0x01 — Read Coils.
 *
 * C reads `address` and `quantity`; writes packed coil bits into `out_data` (at most
 * `out_data_len` bytes) and sets `out_byte_count` to the number of bytes written.
 */
typedef struct MbusServerReadCoilsReq {
    /**
     * Slave/unit address of the request.
     */
    uint8_t unit_id;
    /**
     * Transaction ID (MBAP) or 0 for serial.
     */
    uint16_t txn_id;
    /**
     * Starting coil address.
     */
    uint16_t address;
    /**
     * Number of coils to read.
     */
    uint16_t quantity;
    /**
     * Buffer to write packed coil bytes into (Rust-allocated, valid for callback duration).
     */
    uint8_t *out_data;
    /**
     * Capacity of `out_data` in bytes.
     */
    uintptr_t out_data_len;
    /**
     * C must set this to the number of bytes written into `out_data`.
     */
    uint8_t out_byte_count;
} MbusServerReadCoilsReq;
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII)))) && defined(MBUS_FEATURE_COILS))
/**
 * C callback type for FC 0x01.
 */
typedef enum MbusServerExceptionCode (*MbusServerReadCoilsFn)(struct MbusServerReadCoilsReq *req,
                                                              void *userdata);
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII)))) && defined(MBUS_FEATURE_COILS))
/**
 * Request context for FC 0x05 — Write Single Coil.
 */
typedef struct MbusServerWriteSingleCoilReq {
    uint8_t unit_id;
    uint16_t txn_id;
    uint16_t address;
    /**
     * `true` = force coil ON, `false` = force coil OFF.
     */
    bool value;
} MbusServerWriteSingleCoilReq;
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII)))) && defined(MBUS_FEATURE_COILS))
/**
 * C callback type for FC 0x05.
 */
typedef enum MbusServerExceptionCode (*MbusServerWriteSingleCoilFn)(const struct MbusServerWriteSingleCoilReq *req,
                                                                    void *userdata);
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII)))) && defined(MBUS_FEATURE_COILS))
/**
 * Request context for FC 0x0F — Write Multiple Coils.
 *
 * `values` is a packed bit array from the master (LSB of first byte = coil at
 * `starting_address`). Valid for the duration of the callback only.
 */
typedef struct MbusServerWriteMultipleCoilsReq {
    uint8_t unit_id;
    uint16_t txn_id;
    uint16_t starting_address;
    uint16_t quantity;
    /**
     * Packed coil bits from the master (read-only, valid for callback duration).
     */
    const uint8_t *values;
    /**
     * Number of bytes in `values`.
     */
    uintptr_t values_len;
} MbusServerWriteMultipleCoilsReq;
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII)))) && defined(MBUS_FEATURE_COILS))
/**
 * C callback type for FC 0x0F.
 */
typedef enum MbusServerExceptionCode (*MbusServerWriteMultipleCoilsFn)(const struct MbusServerWriteMultipleCoilsReq *req,
                                                                       void *userdata);
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII)))) && defined(MBUS_FEATURE_DISCRETE_INPUTS))
/**
 * Request context for FC 0x02 — Read Discrete Inputs.
 */
typedef struct MbusServerReadDiscreteInputsReq {
    uint8_t unit_id;
    uint16_t txn_id;
    uint16_t address;
    uint16_t quantity;
    uint8_t *out_data;
    uintptr_t out_data_len;
    uint8_t out_byte_count;
} MbusServerReadDiscreteInputsReq;
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII)))) && defined(MBUS_FEATURE_DISCRETE_INPUTS))
/**
 * C callback type for FC 0x02.
 */
typedef enum MbusServerExceptionCode (*MbusServerReadDiscreteInputsFn)(struct MbusServerReadDiscreteInputsReq *req,
                                                                       void *userdata);
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII)))) && defined(MBUS_FEATURE_HOLDING_REGISTERS))
/**
 * Request context for FC 0x03 — Read Holding Registers.
 *
 * `out_data` receives big-endian register bytes (2 bytes per register).
 * `out_byte_count` must be set to `quantity * 2` on success.
 */
typedef struct MbusServerReadHoldingRegistersReq {
    uint8_t unit_id;
    uint16_t txn_id;
    uint16_t address;
    uint16_t quantity;
    uint8_t *out_data;
    uintptr_t out_data_len;
    uint8_t out_byte_count;
} MbusServerReadHoldingRegistersReq;
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII)))) && defined(MBUS_FEATURE_HOLDING_REGISTERS))
/**
 * C callback type for FC 0x03.
 */
typedef enum MbusServerExceptionCode (*MbusServerReadHoldingRegistersFn)(struct MbusServerReadHoldingRegistersReq *req,
                                                                         void *userdata);
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII)))) && defined(MBUS_FEATURE_HOLDING_REGISTERS))
/**
 * Request context for FC 0x06 — Write Single Register.
 */
typedef struct MbusServerWriteSingleRegisterReq {
    uint8_t unit_id;
    uint16_t txn_id;
    uint16_t address;
    uint16_t value;
} MbusServerWriteSingleRegisterReq;
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII)))) && defined(MBUS_FEATURE_HOLDING_REGISTERS))
/**
 * C callback type for FC 0x06.
 */
typedef enum MbusServerExceptionCode (*MbusServerWriteSingleRegisterFn)(const struct MbusServerWriteSingleRegisterReq *req,
                                                                        void *userdata);
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII)))) && defined(MBUS_FEATURE_HOLDING_REGISTERS))
/**
 * Request context for FC 0x10 — Write Multiple Registers.
 *
 * `values` contains the decoded `u16` register values (big-endian already parsed).
 * Valid for the duration of the callback only.
 */
typedef struct MbusServerWriteMultipleRegistersReq {
    uint8_t unit_id;
    uint16_t txn_id;
    uint16_t starting_address;
    /**
     * Decoded register values from the master (read-only, valid for callback duration).
     */
    const uint16_t *values;
    /**
     * Number of elements in `values`.
     */
    uintptr_t values_len;
} MbusServerWriteMultipleRegistersReq;
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII)))) && defined(MBUS_FEATURE_HOLDING_REGISTERS))
/**
 * C callback type for FC 0x10.
 */
typedef enum MbusServerExceptionCode (*MbusServerWriteMultipleRegistersFn)(const struct MbusServerWriteMultipleRegistersReq *req,
                                                                           void *userdata);
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII)))) && defined(MBUS_FEATURE_HOLDING_REGISTERS))
/**
 * Request context for FC 0x16 — Mask Write Register.
 */
typedef struct MbusServerMaskWriteRegisterReq {
    uint8_t unit_id;
    uint16_t txn_id;
    uint16_t address;
    uint16_t and_mask;
    uint16_t or_mask;
} MbusServerMaskWriteRegisterReq;
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII)))) && defined(MBUS_FEATURE_HOLDING_REGISTERS))
/**
 * C callback type for FC 0x16.
 */
typedef enum MbusServerExceptionCode (*MbusServerMaskWriteRegisterFn)(const struct MbusServerMaskWriteRegisterReq *req,
                                                                      void *userdata);
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII)))) && defined(MBUS_FEATURE_HOLDING_REGISTERS))
/**
 * Request context for FC 0x17 — Read/Write Multiple Registers.
 *
 * C writes the read result into `out_data` (big-endian register bytes) and sets
 * `out_byte_count`. `write_values` contains the decoded write values from the master.
 */
typedef struct MbusServerReadWriteMultipleRegistersReq {
    uint8_t unit_id;
    uint16_t txn_id;
    uint16_t read_address;
    uint16_t read_quantity;
    uint16_t write_address;
    /**
     * Decoded write values from the master (read-only, valid for callback duration).
     */
    const uint16_t *write_values;
    uintptr_t write_values_len;
    uint8_t *out_data;
    uintptr_t out_data_len;
    /**
     * C must set to the number of bytes written into `out_data`.
     */
    uint8_t out_byte_count;
} MbusServerReadWriteMultipleRegistersReq;
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII)))) && defined(MBUS_FEATURE_HOLDING_REGISTERS))
/**
 * C callback type for FC 0x17.
 */
typedef enum MbusServerExceptionCode (*MbusServerReadWriteMultipleRegistersFn)(struct MbusServerReadWriteMultipleRegistersReq *req,
                                                                               void *userdata);
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII)))) && defined(MBUS_FEATURE_INPUT_REGISTERS))
/**
 * Request context for FC 0x04 — Read Input Registers.
 */
typedef struct MbusServerReadInputRegistersReq {
    uint8_t unit_id;
    uint16_t txn_id;
    uint16_t address;
    uint16_t quantity;
    uint8_t *out_data;
    uintptr_t out_data_len;
    uint8_t out_byte_count;
} MbusServerReadInputRegistersReq;
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII)))) && defined(MBUS_FEATURE_INPUT_REGISTERS))
/**
 * C callback type for FC 0x04.
 */
typedef enum MbusServerExceptionCode (*MbusServerReadInputRegistersFn)(struct MbusServerReadInputRegistersReq *req,
                                                                       void *userdata);
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII)))) && defined(MBUS_FEATURE_FIFO))
/**
 * Request context for FC 0x18 — Read FIFO Queue.
 */
typedef struct MbusServerReadFifoQueueReq {
    uint8_t unit_id;
    uint16_t txn_id;
    uint16_t pointer_address;
    uint8_t *out_data;
    uintptr_t out_data_len;
    /**
     * C must set to the number of bytes written.
     */
    uint8_t out_byte_count;
} MbusServerReadFifoQueueReq;
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII)))) && defined(MBUS_FEATURE_FIFO))
/**
 * C callback type for FC 0x18.
 */
typedef enum MbusServerExceptionCode (*MbusServerReadFifoQueueFn)(struct MbusServerReadFifoQueueReq *req,
                                                                  void *userdata);
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII)))) && defined(MBUS_FEATURE_FILE_RECORD))
/**
 * Request context for FC 0x14 — Read File Record (one sub-request at a time).
 *
 * C writes record data bytes into `out_data` and sets `out_byte_count`.
 */
typedef struct MbusServerReadFileRecordReq {
    uint8_t unit_id;
    uint16_t txn_id;
    uint16_t file_number;
    uint16_t record_number;
    uint16_t record_length;
    uint8_t *out_data;
    uintptr_t out_data_len;
    /**
     * C must set to the number of bytes written into `out_data`.
     */
    uint8_t out_byte_count;
} MbusServerReadFileRecordReq;
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII)))) && defined(MBUS_FEATURE_FILE_RECORD))
/**
 * C callback type for FC 0x14.
 */
typedef enum MbusServerExceptionCode (*MbusServerReadFileRecordFn)(struct MbusServerReadFileRecordReq *req,
                                                                   void *userdata);
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII)))) && defined(MBUS_FEATURE_FILE_RECORD))
/**
 * Request context for FC 0x15 — Write File Record (one sub-request at a time).
 *
 * `record_data` contains decoded `u16` words. Valid for the callback duration only.
 */
typedef struct MbusServerWriteFileRecordReq {
    uint8_t unit_id;
    uint16_t txn_id;
    uint16_t file_number;
    uint16_t record_number;
    uint16_t record_length;
    /**
     * Decoded record data words (read-only, valid for callback duration).
     */
    const uint16_t *record_data;
    uintptr_t record_data_len;
} MbusServerWriteFileRecordReq;
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII)))) && defined(MBUS_FEATURE_FILE_RECORD))
/**
 * C callback type for FC 0x15.
 */
typedef enum MbusServerExceptionCode (*MbusServerWriteFileRecordFn)(const struct MbusServerWriteFileRecordReq *req,
                                                                    void *userdata);
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII)))) && defined(MBUS_FEATURE_DIAGNOSTICS))
/**
 * Request context for FC 0x07 — Read Exception Status.
 *
 * C sets `out_status` to the 8-bit exception status byte.
 */
typedef struct MbusServerReadExceptionStatusReq {
    uint8_t unit_id;
    uint16_t txn_id;
    /**
     * C must set this to the 8-bit exception status byte.
     */
    uint8_t out_status;
} MbusServerReadExceptionStatusReq;
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII)))) && defined(MBUS_FEATURE_DIAGNOSTICS))
/**
 * C callback type for FC 0x07.
 */
typedef enum MbusServerExceptionCode (*MbusServerReadExceptionStatusFn)(struct MbusServerReadExceptionStatusReq *req,
                                                                        void *userdata);
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII)))) && defined(MBUS_FEATURE_DIAGNOSTICS))
/**
 * Request context for FC 0x08 — Diagnostics.
 *
 * `sub_function` is the raw 16-bit sub-function code (see Modbus spec §6.8).
 * C sets `out_result` to the 16-bit data echo/result.
 */
typedef struct MbusServerDiagnosticsReq {
    uint8_t unit_id;
    uint16_t txn_id;
    /**
     * Raw sub-function code (e.g. 0x0000 = Return Query Data, 0x0002 = Return Diagnostic Register).
     */
    uint16_t sub_function;
    /**
     * Data word from the request.
     */
    uint16_t data;
    /**
     * C must set this to the 16-bit result/echo value.
     */
    uint16_t out_result;
} MbusServerDiagnosticsReq;
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII)))) && defined(MBUS_FEATURE_DIAGNOSTICS))
/**
 * C callback type for FC 0x08.
 */
typedef enum MbusServerExceptionCode (*MbusServerDiagnosticsFn)(struct MbusServerDiagnosticsReq *req,
                                                                void *userdata);
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII)))) && defined(MBUS_FEATURE_DIAGNOSTICS))
/**
 * Request context for FC 0x0B — Get Comm Event Counter.
 *
 * C sets `out_status` (status word) and `out_event_count`.
 */
typedef struct MbusServerGetCommEventCounterReq {
    uint8_t unit_id;
    uint16_t txn_id;
    /**
     * C must set this to the status word.
     */
    uint16_t out_status;
    /**
     * C must set this to the event counter value.
     */
    uint16_t out_event_count;
} MbusServerGetCommEventCounterReq;
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII)))) && defined(MBUS_FEATURE_DIAGNOSTICS))
/**
 * C callback type for FC 0x0B.
 */
typedef enum MbusServerExceptionCode (*MbusServerGetCommEventCounterFn)(struct MbusServerGetCommEventCounterReq *req,
                                                                        void *userdata);
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII)))) && defined(MBUS_FEATURE_DIAGNOSTICS))
/**
 * Request context for FC 0x0C — Get Comm Event Log.
 *
 * C writes event bytes into `out_events` (max `out_events_len` bytes) and
 * sets the four output scalar fields.
 */
typedef struct MbusServerGetCommEventLogReq {
    uint8_t unit_id;
    uint16_t txn_id;
    /**
     * Buffer for event log bytes (Rust-allocated, valid for callback duration).
     */
    uint8_t *out_events;
    /**
     * Capacity of `out_events`.
     */
    uintptr_t out_events_len;
    /**
     * C must set to the Status word.
     */
    uint16_t out_status;
    /**
     * C must set to the Event Count.
     */
    uint16_t out_event_count;
    /**
     * C must set to the Message Count.
     */
    uint16_t out_message_count;
    /**
     * C must set to the number of events written into `out_events`.
     */
    uint8_t out_num_events;
} MbusServerGetCommEventLogReq;
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII)))) && defined(MBUS_FEATURE_DIAGNOSTICS))
/**
 * C callback type for FC 0x0C.
 */
typedef enum MbusServerExceptionCode (*MbusServerGetCommEventLogFn)(struct MbusServerGetCommEventLogReq *req,
                                                                    void *userdata);
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII)))) && defined(MBUS_FEATURE_DIAGNOSTICS))
/**
 * Request context for FC 0x11 — Report Server ID.
 *
 * C writes vendor/device identification bytes into `out_server_id` and sets
 * `out_byte_count` and `out_run_indicator_status`.
 */
typedef struct MbusServerReportServerIdReq {
    uint8_t unit_id;
    uint16_t txn_id;
    /**
     * Buffer for server ID bytes (Rust-allocated).
     */
    uint8_t *out_server_id;
    /**
     * Capacity of `out_server_id`.
     */
    uintptr_t out_server_id_len;
    /**
     * C must set to the number of Server ID bytes written.
     */
    uint8_t out_byte_count;
    /**
     * C must set to the Run Indicator Status byte (0x00 = OFF, 0xFF = ON).
     */
    uint8_t out_run_indicator_status;
} MbusServerReportServerIdReq;
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII)))) && defined(MBUS_FEATURE_DIAGNOSTICS))
/**
 * C callback type for FC 0x11.
 */
typedef enum MbusServerExceptionCode (*MbusServerReportServerIdFn)(struct MbusServerReportServerIdReq *req,
                                                                   void *userdata);
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII)))) && defined(MBUS_FEATURE_DIAGNOSTICS))
/**
 * Request context for FC 0x2B / MEI 0x0E — Read Device Identification.
 *
 * C writes object data into `out_data` and sets all `out_*` fields.
 */
typedef struct MbusServerReadDeviceIdentificationReq {
    uint8_t unit_id;
    uint16_t txn_id;
    uint8_t read_device_id_code;
    uint8_t start_object_id;
    uint8_t *out_data;
    uintptr_t out_data_len;
    /**
     * C must set to the conformity level byte.
     */
    uint8_t out_conformity_level;
    /**
     * C must set to the "more follows" object ID (0x00 if none).
     */
    uint8_t out_more_follows_object_id;
    /**
     * C must set to `true` if more objects follow (segmentation).
     */
    bool out_has_more;
    /**
     * C must set to the next Object ID to request (only valid when `out_has_more` is true).
     */
    uint8_t out_next_object_id;
    /**
     * C must set to the number of bytes written into `out_data`.
     */
    uint8_t out_byte_count;
} MbusServerReadDeviceIdentificationReq;
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII)))) && defined(MBUS_FEATURE_DIAGNOSTICS))
/**
 * C callback type for FC 0x2B / MEI 0x0E.
 */
typedef enum MbusServerExceptionCode (*MbusServerReadDeviceIdentificationFn)(struct MbusServerReadDeviceIdentificationReq *req,
                                                                             void *userdata);
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII))))
/**
 * Master callback table for a C Modbus server application.
 *
 * Pass a populated instance to `mbus_tcp_server_new`,
 * `mbus_serial_rtu_server_new`, or `mbus_serial_ascii_server_new`.
 *
 * ## NULL slots
 * Any callback slot left as `NULL` causes the server to respond with
 * `ExceptionCode::IllegalFunction` (0x01) for that function code.
 * You do not need to implement callbacks for function codes your device
 * does not support.
 *
 * ## Return value contract
 * Every callback returns `MbusServerExceptionCode`.  The server uses this to
 * decide what to send on the wire:
 *
 * | Return value | Wire response |
 * |---|---|
 * | `Ok` | Normal success response (echo or `out_*` data). |
 * | `IllegalFunction` | Exception response 0x01. |
 * | `IllegalDataAddress` | Exception response 0x02. |
 * | `IllegalDataValue` | Exception response 0x03. |
 * | `ServerDeviceFailure` | Exception response 0x04. |
 *
 * ## Timing — serial servers
 * Callbacks are invoked from within `mbus_serial_server_poll`.  The stack
 * does **not** call the `recv` callback while a callback is running, so you
 * cannot observe a t1.5/t3.5 violation from inside a handler.  Timing
 * violations are detected and reported exclusively through the `recv`
 * transport callback before dispatch (see the `serial_server` module docs).
 *
 * ## Lifetime of pointer fields
 * All `*mut` and `*const` pointer fields in the `*Req` structs are valid only
 * for the duration of the callback invocation.  Do **not** retain them.
 *
 * ## Example (C)
 *
 * ```c
 * static MbusServerHandlers g_handlers = {
 *     .userdata             = &my_app_state,
 *     .on_read_holding_registers = my_read_hr_handler,
 *     .on_write_single_register  = my_write_sr_handler,
 *     // All other slots are NULL → server returns IllegalFunction.
 * };
 * ```
 */
typedef struct MbusServerHandlers {
    /**
     * Opaque pointer passed to every callback.
     */
    void *userdata;
#if defined(MBUS_FEATURE_COILS)
    MbusServerReadCoilsFn on_read_coils
#endif
    ;
#if defined(MBUS_FEATURE_COILS)
    MbusServerWriteSingleCoilFn on_write_single_coil
#endif
    ;
#if defined(MBUS_FEATURE_COILS)
    MbusServerWriteMultipleCoilsFn on_write_multiple_coils
#endif
    ;
#if defined(MBUS_FEATURE_DISCRETE_INPUTS)
    MbusServerReadDiscreteInputsFn on_read_discrete_inputs
#endif
    ;
#if defined(MBUS_FEATURE_HOLDING_REGISTERS)
    MbusServerReadHoldingRegistersFn on_read_holding_registers
#endif
    ;
#if defined(MBUS_FEATURE_HOLDING_REGISTERS)
    MbusServerWriteSingleRegisterFn on_write_single_register
#endif
    ;
#if defined(MBUS_FEATURE_HOLDING_REGISTERS)
    MbusServerWriteMultipleRegistersFn on_write_multiple_registers
#endif
    ;
#if defined(MBUS_FEATURE_HOLDING_REGISTERS)
    MbusServerMaskWriteRegisterFn on_mask_write_register
#endif
    ;
#if defined(MBUS_FEATURE_HOLDING_REGISTERS)
    MbusServerReadWriteMultipleRegistersFn on_read_write_multiple_registers
#endif
    ;
#if defined(MBUS_FEATURE_INPUT_REGISTERS)
    MbusServerReadInputRegistersFn on_read_input_registers
#endif
    ;
#if defined(MBUS_FEATURE_FIFO)
    MbusServerReadFifoQueueFn on_read_fifo_queue
#endif
    ;
#if defined(MBUS_FEATURE_FILE_RECORD)
    MbusServerReadFileRecordFn on_read_file_record
#endif
    ;
#if defined(MBUS_FEATURE_FILE_RECORD)
    MbusServerWriteFileRecordFn on_write_file_record
#endif
    ;
#if defined(MBUS_FEATURE_DIAGNOSTICS)
    MbusServerReadExceptionStatusFn on_read_exception_status
#endif
    ;
#if defined(MBUS_FEATURE_DIAGNOSTICS)
    MbusServerDiagnosticsFn on_diagnostics
#endif
    ;
#if defined(MBUS_FEATURE_DIAGNOSTICS)
    MbusServerGetCommEventCounterFn on_get_comm_event_counter
#endif
    ;
#if defined(MBUS_FEATURE_DIAGNOSTICS)
    MbusServerGetCommEventLogFn on_get_comm_event_log
#endif
    ;
#if defined(MBUS_FEATURE_DIAGNOSTICS)
    MbusServerReportServerIdFn on_report_server_id
#endif
    ;
#if defined(MBUS_FEATURE_DIAGNOSTICS)
    MbusServerReadDeviceIdentificationFn on_read_device_identification
#endif
    ;
} MbusServerHandlers;
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII))))
/**
 * Common server configuration shared by TCP and Serial server types.
 *
 * Only fields that influence the server runtime logic are included here;
 * transport-level details (host address, port, baud rate) are handled
 * separately in the `_new` constructor callbacks.
 */
typedef struct MbusServerConfig {
    /**
     * Slave/unit address this server will respond to.  Valid range: 1–247.
     * For TCP-only deployments the conventional value is 1 or 0xFF.
     */
    uint8_t slave_address;
    /**
     * Maximum time (ms) allocated to process and respond to a single request.
     * Passed to `ResilienceConfig::default()` for now; reserved for future
     * per-request timeout enforcement.
     */
    uint32_t response_timeout_ms;
} MbusServerConfig;
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_GATEWAY)) && defined(MBUS_FEATURE_C_GATEWAY))
/**
 * Observability callbacks delivered by the gateway.
 *
 * Pass a populated value to [`mbus_gateway_new`](super::gateway::mbus_gateway_new).
 * Any field set to `None` (`NULL` from C) is treated as a no-op.
 */
typedef struct MbusGatewayCallbacks {
    /**
     * Opaque user pointer threaded to every callback.
     */
    void *userdata;
    /**
     * Called after a request has been forwarded to a downstream channel.
     */
    void (*on_forward)(uint8_t session_id, uint8_t unit_id, uint16_t channel_idx, void *userdata);
    /**
     * Called after a response has been delivered to the upstream client.
     */
    void (*on_response_returned)(uint8_t session_id, uint16_t upstream_txn, void *userdata);
    /**
     * Called when no downstream route was found for a unit ID.
     */
    void (*on_routing_miss)(uint8_t session_id, uint8_t unit_id, void *userdata);
    /**
     * Called when a downstream device failed to respond before timeout.
     */
    void (*on_downstream_timeout)(uint8_t session_id, uint16_t internal_txn, void *userdata);
    /**
     * Called when the upstream session disconnects (transport-level error).
     */
    void (*on_upstream_disconnect)(uint8_t session_id, void *userdata);
} MbusGatewayCallbacks;
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_GATEWAY))
/**
 * Opaque gateway handle. `MBUS_INVALID_GATEWAY_ID` (0xFF) is the sentinel.
 */
typedef uint8_t MbusGatewayId;
#endif

#if (defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32))
/**
 * Status code returned by every `mbus_dn_*` function.
 *
 * Numerically identical to [`crate::c::error::MbusStatusCode`].
 */
typedef enum MbusStatusCode MbusDnStatus;
#endif

#if ((defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32)) && defined(MBUS_FEATURE_FILE_RECORD))
/**
 * A sub-request descriptor for serial file record operations.
 *
 * Identical layout to `MbusDnSubRequest` in `tcp.rs` — shared by both
 * transport variants so C# can use the same struct definition.
 */
typedef struct MbusDnSerialSubRequest {
    /**
     * File number (1–65535).
     */
    uint16_t file_number;
    /**
     * Starting record number within the file (0–9999).
     */
    uint16_t record_number;
    /**
     * Number of 16-bit registers. For reads: how many to read; for writes: must equal `data_len`.
     */
    uint16_t record_length;
    /**
     * Pointer to write data (`null` for reads).
     */
    const uint16_t *data;
    /**
     * Number of valid words pointed to by `data` (0 for reads).
     */
    uint16_t data_len;
} MbusDnSerialSubRequest;
#endif

#if ((defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32)) && defined(MBUS_FEATURE_FILE_RECORD))
/**
 * A sub-request descriptor used by [`mbus_dn_tcp_client_read_file_record`] and
 * [`mbus_dn_tcp_client_write_file_record`].
 *
 * **Read** sub-requests: set `data` to `null` and `data_len` to `0`; fill
 * `file_number`, `record_number`, and `record_length` with the target record.
 *
 * **Write** sub-requests: set `data` to a pointer to `data_len` `u16` words and
 * ensure `record_length == data_len`.
 */
typedef struct MbusDnSubRequest {
    /**
     * File number (1–65535).
     */
    uint16_t file_number;
    /**
     * Starting record number within the file (0–9999).
     */
    uint16_t record_number;
    /**
     * Number of 16-bit registers. For reads: how many to read; for writes: must equal `data_len`.
     */
    uint16_t record_length;
    /**
     * Pointer to write data (`null` for reads). Valid for at least `data_len` words.
     */
    const uint16_t *data;
    /**
     * Number of valid words pointed to by `data` (0 for reads).
     */
    uint16_t data_len;
} MbusDnSubRequest;
#endif

#if (defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32))
/**
 * C-compatible vtable of optional Modbus request handler callbacks.
 *
 * Set any slot to `None` (`null` from C#) to return an `IllegalFunction`
 * exception for that function code.  All function pointers receive `ctx` as
 * their first argument.
 *
 * # Repr
 *
 * The struct uses `#[repr(C)]` so that it has a stable, platform-defined
 * layout.  Pass it by pointer to `mbus_dn_tcp_server_new`.
 */
typedef struct MbusDnServerVtable {
    /**
     * Caller-supplied opaque context forwarded unchanged to every callback.
     */
    void *ctx;
#if defined(MBUS_FEATURE_COILS)
    /**
     * `fn(ctx, address, count, out_packed_bytes, out_byte_count) -> i32`
     */
    int32_t (*read_coils)(void*, uint16_t, uint16_t, uint8_t*, uint16_t*)
#endif
    ;
#if defined(MBUS_FEATURE_COILS)
    /**
     * `fn(ctx, address, value_bool) -> i32`
     */
    int32_t (*write_single_coil)(void*, uint16_t, uint8_t)
#endif
    ;
#if defined(MBUS_FEATURE_COILS)
    /**
     * `fn(ctx, address, packed_bytes, byte_count, coil_count) -> i32`
     */
    int32_t (*write_multiple_coils)(void*, uint16_t, const uint8_t*, uint16_t, uint16_t)
#endif
    ;
#if defined(MBUS_FEATURE_DISCRETE_INPUTS)
    /**
     * `fn(ctx, address, count, out_packed_bytes, out_byte_count) -> i32`
     */
    int32_t (*read_discrete_inputs)(void*, uint16_t, uint16_t, uint8_t*, uint16_t*)
#endif
    ;
    /**
     * `fn(ctx, address, count, out_u16_values, out_count) -> i32`
     */
    int32_t (*read_holding_registers)(void*, uint16_t, uint16_t, uint16_t*, uint16_t*);
    /**
     * `fn(ctx, address, count, out_u16_values, out_count) -> i32`
     */
    int32_t (*read_input_registers)(void*, uint16_t, uint16_t, uint16_t*, uint16_t*);
    /**
     * `fn(ctx, address, value) -> i32`
     */
    int32_t (*write_single_register)(void*, uint16_t, uint16_t);
    /**
     * `fn(ctx, address, values_be_bytes, count) -> i32`
     */
    int32_t (*write_multiple_registers)(void*, uint16_t, const uint8_t*, uint16_t);
    /**
     * `fn(ctx, address, and_mask, or_mask) -> i32`
     */
    int32_t (*mask_write_register)(void*, uint16_t, uint16_t, uint16_t);
    /**
     * `fn(ctx, read_addr, read_count, write_addr, write_values_be_bytes,
     *    write_count, out_u16_values, out_count) -> i32`
     */
    int32_t (*read_write_multiple_registers)(void*,
                                             uint16_t,
                                             uint16_t,
                                             uint16_t,
                                             const uint8_t*,
                                             uint16_t,
                                             uint16_t*,
                                             uint16_t*);
#if defined(MBUS_FEATURE_FIFO)
    /**
     * `fn(ctx, pointer_address, out_u16_values, out_count) -> i32`
     */
    int32_t (*read_fifo_queue)(void*, uint16_t, uint16_t*, uint16_t*)
#endif
    ;
#if defined(MBUS_FEATURE_DIAGNOSTICS)
    /**
     * `fn(ctx, out_status_byte) -> i32`
     */
    int32_t (*read_exception_status)(void*, uint8_t*)
#endif
    ;
#if defined(MBUS_FEATURE_DIAGNOSTICS)
    /**
     * `fn(ctx, sub_fn, data, out_sub_fn, out_data) -> i32`
     */
    int32_t (*diagnostics)(void*, uint16_t, uint16_t, uint16_t*, uint16_t*)
#endif
    ;
#if defined(MBUS_FEATURE_DIAGNOSTICS)
    /**
     * `fn(ctx, out_status_word, out_event_count) -> i32`
     */
    int32_t (*get_comm_event_counter)(void*, uint16_t*, uint16_t*)
#endif
    ;
#if defined(MBUS_FEATURE_DIAGNOSTICS)
    /**
     * `fn(ctx, out_payload_bytes, out_byte_count) -> i32`
     *
     * The payload must be formatted as:
     * `[status_hi, status_lo, event_count_hi, event_count_lo,
     *   msg_count_hi, msg_count_lo, event_bytes...]`
     */
    int32_t (*get_comm_event_log)(void*, uint8_t*, uint16_t*)
#endif
    ;
#if defined(MBUS_FEATURE_DIAGNOSTICS)
    /**
     * `fn(ctx, out_payload_bytes, out_byte_count) -> i32`
     */
    int32_t (*report_server_id)(void*, uint8_t*, uint16_t*)
#endif
    ;
} MbusDnServerVtable;
#endif

#if (defined(MBUS_FEATURE_GO) && !defined(MBUS_TARGET_WASM32))
/**
 * Status code returned by every `mbus_go_*` function.
 *
 * Numerically identical to [`crate::c::error::MbusStatusCode`].
 */
typedef enum MbusStatusCode MbusGoStatus;
#endif

#if ((defined(MBUS_FEATURE_GO) && !defined(MBUS_TARGET_WASM32)) && defined(MBUS_FEATURE_FILE_RECORD))
/**
 * A sub-request descriptor for serial file record operations.
 *
 * Identical layout to `MbusGoSubRequest` in `tcp.rs` — shared by both
 * transport variants so Go can use the same struct definition.
 */
typedef struct MbusGoSerialSubRequest {
    /**
     * File number (1–65535).
     */
    uint16_t file_number;
    /**
     * Starting record number within the file (0–9999).
     */
    uint16_t record_number;
    /**
     * Number of 16-bit registers. For reads: how many to read; for writes: must equal `data_len`.
     */
    uint16_t record_length;
    /**
     * Pointer to write data (`null` for reads).
     */
    const uint16_t *data;
    /**
     * Number of valid words pointed to by `data` (0 for reads).
     */
    uint16_t data_len;
} MbusGoSerialSubRequest;
#endif

#if ((defined(MBUS_FEATURE_GO) && !defined(MBUS_TARGET_WASM32)) && defined(MBUS_FEATURE_FILE_RECORD))
/**
 * A sub-request descriptor used by [`mbus_go_tcp_client_read_file_record`] and
 * [`mbus_go_tcp_client_write_file_record`].
 *
 * **Read** sub-requests: set `data` to `null` and `data_len` to `0`; fill
 * `file_number`, `record_number`, and `record_length` with the target record.
 *
 * **Write** sub-requests: set `data` to a pointer to `data_len` `u16` words and
 * ensure `record_length == data_len`.
 */
typedef struct MbusGoSubRequest {
    /**
     * File number (1–65535).
     */
    uint16_t file_number;
    /**
     * Starting record number within the file (0–9999).
     */
    uint16_t record_number;
    /**
     * Number of 16-bit registers. For reads: how many to read; for writes: must equal `data_len`.
     */
    uint16_t record_length;
    /**
     * Pointer to write data (`null` for reads). Valid for at least `data_len` words.
     */
    const uint16_t *data;
    /**
     * Number of valid words pointed to by `data` (0 for reads).
     */
    uint16_t data_len;
} MbusGoSubRequest;
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT))
/**
 * Sentinel value meaning "no valid client".
 */
#define MBUS_INVALID_CLIENT_ID 65535
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII))))
/**
 * Sentinel value meaning "no valid server" / creation failed.
 */
#define MBUS_INVALID_SERVER_ID 65535
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_GATEWAY))
/**
 * Sentinel "no valid gateway" value.
 */
#define MBUS_INVALID_GATEWAY_ID 255
#endif

#if (defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO))
/**
 * Returns a static null-terminated C string describing the status code.
 *
 * The returned pointer is always valid (points to a static string literal).
 * The caller must NOT free it.
 */
const char *mbus_status_str(enum MbusStatusCode code);
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_C_GATEWAY)))
/**
 * No-op stub for pool lock (used when no external C caller provides it).
 */
void mbus_pool_lock(void);
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_C_GATEWAY)))
/**
 * No-op stub for pool unlock.
 */
void mbus_pool_unlock(void);
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT))
/**
 * No-op stub for client lock.
 */
void mbus_client_lock(uint16_t _id);
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT))
/**
 * No-op stub for client unlock.
 */
void mbus_client_unlock(uint16_t _id);
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_SERVER))
/**
 * No-op stub for server lock.
 */
void mbus_server_lock(uint16_t _id);
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_SERVER))
/**
 * No-op stub for server unlock.
 */
void mbus_server_unlock(uint16_t _id);
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_GATEWAY))
/**
 * No-op stub for gateway lock.
 */
void mbus_gateway_lock(uint8_t _id);
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_GATEWAY))
/**
 * No-op stub for gateway unlock.
 */
void mbus_gateway_unlock(uint8_t _id);
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT)) && defined(MBUS_FEATURE_COILS))
/**
 * Returns the starting address of the coils range.
 *
 * # Safety
 * `coils` must either be null (returns 0) or point to a valid `MbusCoils`.
 */
uint16_t mbus_coils_from_address(const MbusCoils *coils);
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT)) && defined(MBUS_FEATURE_COILS))
/**
 * Returns the number of coils.
 *
 * # Safety
 * `coils` must either be null (returns 0) or point to a valid `MbusCoils`.
 */
uint16_t mbus_coils_quantity(const MbusCoils *coils);
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT)) && defined(MBUS_FEATURE_COILS))
/**
 * Reads a single coil value by address into `out_value`.
 *
 * # Safety
 * `coils` and `out_value` must be non-null and point to valid memory, or null
 * (returns `MBUS_ERR_NULL_POINTER` for null).
 */
enum MbusStatusCode mbus_coils_value(const MbusCoils *coils,
                                     uint16_t address,
                                     bool *out_value);
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT)) && defined(MBUS_FEATURE_COILS))
/**
 * Reads a single coil value by 0-based index into `out_value`.
 *
 * `index` must be less than [`mbus_coils_quantity`]; otherwise returns
 * `MBUS_ERR_INVALID_ADDRESS`. Unlike [`mbus_coils_value`], no knowledge of
 * the Modbus starting address is required.
 *
 * # Safety
 * `coils` and `out_value` must be non-null and point to valid memory, or null
 * (returns `MBUS_ERR_NULL_POINTER` for null).
 */
enum MbusStatusCode mbus_coils_value_at_index(const MbusCoils *coils,
                                              uint16_t index,
                                              bool *out_value);
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT)) && defined(MBUS_FEATURE_COILS))
/**
 * Returns a raw pointer to the packed coil bit-values. Valid during callback only.
 *
 * # Safety
 * `coils` must either be null (returns a null pointer) or point to a valid `MbusCoils`.
 */
const uint8_t *mbus_coils_values_ptr(const MbusCoils *coils);
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT)) && defined(MBUS_FEATURE_DISCRETE_INPUTS))
/**
 * Returns the starting address of the discrete inputs range.
 *
 * # Safety
 * `discrete_inputs` must either be null (returns 0) or point to a valid `MbusDiscreteInputs`.
 */
uint16_t mbus_discrete_inputs_from_address(const MbusDiscreteInputs *discrete_inputs);
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT)) && defined(MBUS_FEATURE_DISCRETE_INPUTS))
/**
 * Returns the number of discrete inputs.
 *
 * # Safety
 * `discrete_inputs` must either be null (returns 0) or point to a valid `MbusDiscreteInputs`.
 */
uint16_t mbus_discrete_inputs_quantity(const MbusDiscreteInputs *discrete_inputs);
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT)) && defined(MBUS_FEATURE_DISCRETE_INPUTS))
/**
 * Reads a single discrete input value by address into `out_value`.
 *
 * # Safety
 * `discrete_inputs` and `out_value` must be non-null and point to valid memory, or null
 * (returns `MBUS_ERR_NULL_POINTER` for null).
 */
enum MbusStatusCode mbus_discrete_inputs_value(const MbusDiscreteInputs *discrete_inputs,
                                               uint16_t address,
                                               bool *out_value);
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT)) && defined(MBUS_FEATURE_DISCRETE_INPUTS))
/**
 * Reads a single discrete input value by 0-based index into `out_value`.
 *
 * `index` must be less than [`mbus_discrete_inputs_quantity`]; otherwise returns
 * `MBUS_ERR_INVALID_ADDRESS`. Unlike [`mbus_discrete_inputs_value`], no knowledge
 * of the Modbus starting address is required.
 *
 * # Safety
 * `discrete_inputs` and `out_value` must be non-null and point to valid memory, or null
 * (returns `MBUS_ERR_NULL_POINTER` for null).
 */
enum MbusStatusCode mbus_discrete_inputs_value_at_index(const MbusDiscreteInputs *discrete_inputs,
                                                        uint16_t index,
                                                        bool *out_value);
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT)) && defined(MBUS_FEATURE_DISCRETE_INPUTS))
/**
 * Returns a raw pointer to the discrete input bit-values. Valid during callback only.
 *
 * # Safety
 * `discrete_inputs` must either be null (returns a null pointer) or point to a valid
 * `MbusDiscreteInputs`.
 */
const uint8_t *mbus_discrete_inputs_values_ptr(const MbusDiscreteInputs *discrete_inputs);
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT)) && defined(MBUS_FEATURE_FIFO))
/**
 * Returns the FIFO pointer address.
 *
 * # Safety
 * `fifo_queue` must either be null (returns 0) or point to a valid `MbusFifoQueue`.
 */
uint16_t mbus_fifo_queue_ptr_address(const MbusFifoQueue *fifo_queue);
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT)) && defined(MBUS_FEATURE_FIFO))
/**
 * Returns the number of values in the FIFO queue.
 *
 * # Safety
 * `fifo_queue` must either be null (returns 0) or point to a valid `MbusFifoQueue`.
 */
uint16_t mbus_fifo_queue_count(const MbusFifoQueue *fifo_queue);
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT)) && defined(MBUS_FEATURE_FIFO))
/**
 * Reads a FIFO value by index.
 *
 * # Safety
 * `fifo_queue` and `out_value` must be non-null and point to valid memory, or null
 * (returns `MBUS_ERR_NULL_POINTER` for null).
 */
enum MbusStatusCode mbus_fifo_queue_value(const MbusFifoQueue *fifo_queue,
                                          uint16_t index,
                                          uint16_t *out_value);
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT)) && defined(MBUS_FEATURE_FIFO))
/**
 * Returns a raw pointer to the FIFO values. Valid during callback only.
 *
 * # Safety
 * `fifo_queue` must either be null (returns a null pointer) or point to a valid `MbusFifoQueue`.
 */
const uint16_t *mbus_fifo_queue_values_ptr(const MbusFifoQueue *fifo_queue);
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT)) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII)))
/**
 * Lock the global pool (used only during client creation/destruction).
 */
extern void mbus_pool_lock(void);
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT)) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII)))
/**
 * Unlock the global pool.
 */
extern void mbus_pool_unlock(void);
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT)) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII)))
/**
 * Lock a specific client instance (used continuously during polling and requests).
 */
extern void mbus_client_lock(MbusClientId id);
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT)) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII)))
/**
 * Unlock a specific client instance.
 */
extern void mbus_client_unlock(MbusClientId id);
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT) && (defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII)))
/**
 * Create a new Modbus Serial client.
 *
 * Returns a `MbusClientId` on success, or `MBUS_INVALID_CLIENT_ID` on failure.
 *
 * # Safety
 * `config`, `transport_callbacks`, and `callbacks` must be valid non-null
 * pointers for the duration of this call. They are not retained after the
 * call returns.
 */
MbusClientId mbus_serial_client_new(const struct MbusSerialConfig *config,
                                    const struct MbusTransportCallbacks *transport_callbacks,
                                    const struct MbusCallbacks *callbacks);
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT) && (defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII)))
/**
 * Free a Modbus Serial client created by [`mbus_serial_client_new`].
 *
 * After this call the ID is invalid and must not be used.
 * Passing `MBUS_INVALID_CLIENT_ID` is a no-op.
 */
void mbus_serial_client_free(MbusClientId id);
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT) && (defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII)))
/**
 * Open the serial port with the configured parameters.
 */
enum MbusStatusCode mbus_serial_connect(MbusClientId id);
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT) && (defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII)))
/**
 * Close the serial port.
 *
 * Pending in-flight requests are failed immediately with `MBUS_ERR_CONNECTION_LOST`.
 * The client ID remains valid; call [`mbus_serial_connect`] to reopen the port.
 */
enum MbusStatusCode mbus_serial_disconnect(MbusClientId id);
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT) && (defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII)))
/**
 * Returns `1` if the serial port is currently open, `0` otherwise.
 */
uint8_t mbus_serial_is_connected(MbusClientId id);
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT) && (defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII)))
/**
 * Drive the serial Modbus state machine once.
 *
 * Call periodically from your application loop. All registered callbacks are
 * invoked synchronously from within this call.
 */
void mbus_serial_poll(MbusClientId id);
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT) && (defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII)))
/**
 * Returns `1` if the client has in-flight requests awaiting response/timeout,
 * `0` otherwise.
 */
uint8_t mbus_serial_has_pending_requests(MbusClientId id);
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT) && (defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII)))
/**
 * Disconnect then reconnect the serial port.
 */
enum MbusStatusCode mbus_serial_reconnect(MbusClientId id);
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT) && defined(MBUS_FEATURE_NETWORK_TCP))
/**
 * Create a new Modbus TCP client.
 *
 * - `config`    — Must be a valid, non-null pointer to an initialised [`MbusTcpConfig`].
 * - `transport_callbacks` — Must define connect/disconnect/send/recv/is_connected.
 * - `callbacks` — Must be a valid, non-null pointer to an initialised [`MbusCallbacks`].
 *
 * Returns a numeric `MbusClientId` on success, or `MBUS_INVALID_CLIENT_ID`
 * (0xFF) on failure (e.g. invalid config, pool full).
 *
 * # Safety
 * `config`, `transport_callbacks`, and `callbacks` must be valid pointers for
 * the duration of this call. They are not retained after the call returns.
 */
MbusClientId mbus_tcp_client_new(const struct MbusTcpConfig *config,
                                 const struct MbusTransportCallbacks *transport_callbacks,
                                 const struct MbusCallbacks *callbacks);
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT) && defined(MBUS_FEATURE_NETWORK_TCP))
/**
 * Free a Modbus TCP client created by [`mbus_tcp_client_new`].
 *
 * After this call the ID is invalid and must not be used.
 * Passing `MBUS_INVALID_CLIENT_ID` is a no-op.
 */
void mbus_tcp_client_free(MbusClientId id);
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT) && defined(MBUS_FEATURE_NETWORK_TCP))
/**
 * Open the TCP connection to the configured host:port.
 *
 * Returns `MBUS_OK` on success or a specific error code on failure.
 */
enum MbusStatusCode mbus_tcp_connect(MbusClientId id);
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT) && defined(MBUS_FEATURE_NETWORK_TCP))
/**
 * Close the TCP connection.
 *
 * Pending in-flight requests are failed immediately with `MBUS_ERR_CONNECTION_LOST`.
 * The client ID remains valid; call [`mbus_tcp_connect`] to reconnect.
 */
enum MbusStatusCode mbus_tcp_disconnect(MbusClientId id);
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT) && defined(MBUS_FEATURE_NETWORK_TCP))
/**
 * Returns `1` if the TCP connection is currently open, `0` otherwise.
 */
uint8_t mbus_tcp_is_connected(MbusClientId id);
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT) && defined(MBUS_FEATURE_NETWORK_TCP))
/**
 * Drive the Modbus state machine: receive pending frames, match responses to
 * outstanding requests, fire any ready callbacks, and handle timeouts / retries.
 *
 * Call this function periodically (e.g. every 5–20 ms) from your application
 * loop. All registered callbacks are invoked synchronously from within this call.
 */
void mbus_tcp_poll(MbusClientId id);
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT) && defined(MBUS_FEATURE_NETWORK_TCP))
/**
 * Returns `1` if the client has in-flight requests awaiting response/timeout,
 * `0` otherwise.
 */
uint8_t mbus_tcp_has_pending_requests(MbusClientId id);
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT) && defined(MBUS_FEATURE_NETWORK_TCP))
/**
 * Disconnect then reconnect. Useful after a `MBUS_ERR_CONNECTION_LOST` callback.
 */
enum MbusStatusCode mbus_tcp_reconnect(MbusClientId id);
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT) && defined(MBUS_FEATURE_COILS)) && (defined(MBUS_FEATURE_COILS) && defined(MBUS_FEATURE_NETWORK_TCP)))
/**
 * Queue a Read Coils (FC 0x01) request.
 *
 * The response is delivered via `MbusCallbacks::on_read_coils`.
 */
enum MbusStatusCode mbus_tcp_read_coils(MbusClientId id,
                                        uint16_t txn_id,
                                        uint8_t unit_id,
                                        uint16_t address,
                                        uint16_t quantity);
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT) && defined(MBUS_FEATURE_COILS)) && (defined(MBUS_FEATURE_COILS) && (defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII))))
/**
 * Queue a Read Coils (FC 0x01) request.
 *
 * The response is delivered via `MbusCallbacks::on_read_coils`.
 */
enum MbusStatusCode mbus_serial_read_coils(MbusClientId id,
                                           uint16_t txn_id,
                                           uint8_t unit_id,
                                           uint16_t address,
                                           uint16_t quantity);
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT) && defined(MBUS_FEATURE_COILS)) && (defined(MBUS_FEATURE_COILS) && defined(MBUS_FEATURE_NETWORK_TCP)))
/**
 * Queue a Read Single Coil request (reads FC 0x01 with quantity=1).
 */
enum MbusStatusCode mbus_tcp_read_single_coil(MbusClientId id,
                                              uint16_t txn_id,
                                              uint8_t unit_id,
                                              uint16_t address);
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT) && defined(MBUS_FEATURE_COILS)) && (defined(MBUS_FEATURE_COILS) && (defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII))))
/**
 * Queue a Read Single Coil request on a serial client.
 */
enum MbusStatusCode mbus_serial_read_single_coil(MbusClientId id,
                                                 uint16_t txn_id,
                                                 uint8_t unit_id,
                                                 uint16_t address);
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT) && defined(MBUS_FEATURE_COILS)) && (defined(MBUS_FEATURE_COILS) && defined(MBUS_FEATURE_NETWORK_TCP)))
/**
 * Queue a Write Single Coil (FC 0x05) request. `value`: 1 = ON, 0 = OFF.
 */
enum MbusStatusCode mbus_tcp_write_single_coil(MbusClientId id,
                                               uint16_t txn_id,
                                               uint8_t unit_id,
                                               uint16_t address,
                                               uint8_t value);
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT) && defined(MBUS_FEATURE_COILS)) && (defined(MBUS_FEATURE_COILS) && (defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII))))
/**
 * Queue a Write Single Coil (FC 0x05) request on a serial client.
 */
enum MbusStatusCode mbus_serial_write_single_coil(MbusClientId id,
                                                  uint16_t txn_id,
                                                  uint8_t unit_id,
                                                  uint16_t address,
                                                  uint8_t value);
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT) && defined(MBUS_FEATURE_COILS)) && (defined(MBUS_FEATURE_COILS) && defined(MBUS_FEATURE_NETWORK_TCP)))
/**
 * Queue a Write Multiple Coils (FC 0x0F) request.
 *
 * `values` must point to at least `ceil(quantity / 8)` bytes of bit-packed coil
 * data (LSB-first). Valid for the duration of this call only.
 *
 * # Safety
 * `values` must be a valid non-null pointer.
 */
enum MbusStatusCode mbus_tcp_write_multiple_coils(MbusClientId id,
                                                  uint16_t txn_id,
                                                  uint8_t unit_id,
                                                  uint16_t address,
                                                  const uint8_t *values,
                                                  uint16_t quantity);
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT) && defined(MBUS_FEATURE_COILS)) && (defined(MBUS_FEATURE_COILS) && (defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII))))
/**
 * Queue a Write Multiple Coils (FC 0x0F) request on a serial client.
 *
 * # Safety
 * `values` must be a valid non-null pointer.
 */
enum MbusStatusCode mbus_serial_write_multiple_coils(MbusClientId id,
                                                     uint16_t txn_id,
                                                     uint8_t unit_id,
                                                     uint16_t address,
                                                     const uint8_t *values,
                                                     uint16_t quantity);
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT) && defined(MBUS_FEATURE_DIAGNOSTICS)) && (defined(MBUS_FEATURE_DIAGNOSTICS) && defined(MBUS_FEATURE_NETWORK_TCP)))
/**
 * Queue a Diagnostics (FC 0x08) request.
 *
 * # Safety
 * If `data_len > 0`, `data` must be valid for that many `u16` words.
 */
enum MbusStatusCode mbus_tcp_diagnostics(MbusClientId id,
                                         uint16_t txn_id,
                                         uint8_t unit_id,
                                         uint16_t sub_fn,
                                         const uint16_t *data,
                                         uint16_t data_len);
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT) && defined(MBUS_FEATURE_DIAGNOSTICS)) && (defined(MBUS_FEATURE_DIAGNOSTICS) && (defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII))))
/**
 * Queue a Diagnostics (FC 0x08) request on a serial client.
 *
 * # Safety
 * If `data_len > 0`, `data` must be valid for that many `u16` words.
 */
enum MbusStatusCode mbus_serial_diagnostics(MbusClientId id,
                                            uint16_t txn_id,
                                            uint8_t unit_id,
                                            uint16_t sub_fn,
                                            const uint16_t *data,
                                            uint16_t data_len);
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT) && defined(MBUS_FEATURE_DIAGNOSTICS)) && (defined(MBUS_FEATURE_DIAGNOSTICS) && defined(MBUS_FEATURE_NETWORK_TCP)))
/**
 * Queue a Read Device Identification (FC 0x2B/0x0E) request.
 *
 * `dev_id_code`: 1=Basic, 2=Regular, 3=Extended, 4=Specific.
 * `object_id`: starting object ID (0x00–0xFF).
 */
enum MbusStatusCode mbus_tcp_read_device_identification(MbusClientId id,
                                                        uint16_t txn_id,
                                                        uint8_t unit_id,
                                                        uint8_t dev_id_code,
                                                        uint8_t object_id);
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT) && defined(MBUS_FEATURE_DIAGNOSTICS)) && (defined(MBUS_FEATURE_DIAGNOSTICS) && (defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII))))
/**
 * Queue a Read Device Identification request on a serial client.
 */
enum MbusStatusCode mbus_serial_read_device_identification(MbusClientId id,
                                                           uint16_t txn_id,
                                                           uint8_t unit_id,
                                                           uint8_t dev_id_code,
                                                           uint8_t object_id);
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT) && defined(MBUS_FEATURE_DISCRETE_INPUTS)) && (defined(MBUS_FEATURE_DISCRETE_INPUTS) && defined(MBUS_FEATURE_NETWORK_TCP)))
/**
 * Queue a Read Discrete Inputs (FC 0x02) request.
 *
 * The response is delivered via `MbusCallbacks::on_read_discrete_inputs`.
 */
enum MbusStatusCode mbus_tcp_read_discrete_inputs(MbusClientId id,
                                                  uint16_t txn_id,
                                                  uint8_t unit_id,
                                                  uint16_t address,
                                                  uint16_t quantity);
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT) && defined(MBUS_FEATURE_DISCRETE_INPUTS)) && (defined(MBUS_FEATURE_DISCRETE_INPUTS) && (defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII))))
/**
 * Queue a Read Discrete Inputs (FC 0x02) request on a serial client.
 */
enum MbusStatusCode mbus_serial_read_discrete_inputs(MbusClientId id,
                                                     uint16_t txn_id,
                                                     uint8_t unit_id,
                                                     uint16_t address,
                                                     uint16_t quantity);
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT) && defined(MBUS_FEATURE_DISCRETE_INPUTS)) && (defined(MBUS_FEATURE_DISCRETE_INPUTS) && defined(MBUS_FEATURE_NETWORK_TCP)))
/**
 * Queue a Read Single Discrete Input request (FC 0x02 with quantity=1).
 */
enum MbusStatusCode mbus_tcp_read_single_discrete_input(MbusClientId id,
                                                        uint16_t txn_id,
                                                        uint8_t unit_id,
                                                        uint16_t address);
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT) && defined(MBUS_FEATURE_DISCRETE_INPUTS)) && (defined(MBUS_FEATURE_DISCRETE_INPUTS) && (defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII))))
/**
 * Queue a Read Single Discrete Input request on a serial client.
 */
enum MbusStatusCode mbus_serial_read_single_discrete_input(MbusClientId id,
                                                           uint16_t txn_id,
                                                           uint8_t unit_id,
                                                           uint16_t address);
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT) && defined(MBUS_FEATURE_FIFO)) && (defined(MBUS_FEATURE_FIFO) && defined(MBUS_FEATURE_NETWORK_TCP)))
/**
 * Queue a Read FIFO Queue (FC 0x18) request.
 *
 * The response is delivered via `MbusCallbacks::on_read_fifo_queue`.
 */
enum MbusStatusCode mbus_tcp_read_fifo_queue(MbusClientId id,
                                             uint16_t txn_id,
                                             uint8_t unit_id,
                                             uint16_t address);
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT) && defined(MBUS_FEATURE_FIFO)) && (defined(MBUS_FEATURE_FIFO) && (defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII))))
/**
 * Queue a Read FIFO Queue (FC 0x18) request on a serial client.
 */
enum MbusStatusCode mbus_serial_read_fifo_queue(MbusClientId id,
                                                uint16_t txn_id,
                                                uint8_t unit_id,
                                                uint16_t address);
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT) && defined(MBUS_FEATURE_FILE_RECORD)) && (defined(MBUS_FEATURE_FILE_RECORD) && defined(MBUS_FEATURE_NETWORK_TCP)))
/**
 * Queue a Read File Record (FC 0x14) request.
 *
 * # Safety
 * `sub_reqs` must be a valid non-null pointer to `count` items.
 */
enum MbusStatusCode mbus_tcp_read_file_record(MbusClientId id,
                                              uint16_t txn_id,
                                              uint8_t unit_id,
                                              const struct MbusSubRequest *sub_reqs,
                                              uint16_t count);
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT) && defined(MBUS_FEATURE_FILE_RECORD)) && (defined(MBUS_FEATURE_FILE_RECORD) && (defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII))))
/**
 * Queue a Read File Record (FC 0x14) request on a serial client.
 *
 * # Safety
 * `sub_reqs` must be a valid non-null pointer to `count` items.
 */
enum MbusStatusCode mbus_serial_read_file_record(MbusClientId id,
                                                 uint16_t txn_id,
                                                 uint8_t unit_id,
                                                 const struct MbusSubRequest *sub_reqs,
                                                 uint16_t count);
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT) && defined(MBUS_FEATURE_FILE_RECORD)) && (defined(MBUS_FEATURE_FILE_RECORD) && defined(MBUS_FEATURE_NETWORK_TCP)))
/**
 * Queue a Write File Record (FC 0x15) request.
 *
 * # Safety
 * `sub_reqs` must be valid. Each `sub_reqs[i].data` must be valid for
 * `sub_reqs[i].data_len` words.
 */
enum MbusStatusCode mbus_tcp_write_file_record(MbusClientId id,
                                               uint16_t txn_id,
                                               uint8_t unit_id,
                                               const struct MbusSubRequest *sub_reqs,
                                               uint16_t count);
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT) && defined(MBUS_FEATURE_FILE_RECORD)) && (defined(MBUS_FEATURE_FILE_RECORD) && (defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII))))
/**
 * Queue a Write File Record (FC 0x15) request on a serial client.
 *
 * # Safety
 * `sub_reqs` must be valid. Each `sub_reqs[i].data` must be valid for
 * `sub_reqs[i].data_len` words.
 */
enum MbusStatusCode mbus_serial_write_file_record(MbusClientId id,
                                                  uint16_t txn_id,
                                                  uint8_t unit_id,
                                                  const struct MbusSubRequest *sub_reqs,
                                                  uint16_t count);
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT) && (defined(MBUS_FEATURE_HOLDING_REGISTERS) || defined(MBUS_FEATURE_INPUT_REGISTERS))) && (defined(MBUS_FEATURE_HOLDING_REGISTERS) && defined(MBUS_FEATURE_NETWORK_TCP)))
/**
 * Queue a Write Multiple Registers (FC 0x10) request.
 *
 * `values` must point to at least `quantity` `u16` words, valid for this call only.
 *
 * # Safety
 * `values` must be a valid non-null pointer.
 */
enum MbusStatusCode mbus_tcp_write_multiple_registers(MbusClientId id,
                                                      uint16_t txn_id,
                                                      uint8_t unit_id,
                                                      uint16_t address,
                                                      const uint16_t *values,
                                                      uint16_t quantity);
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT) && (defined(MBUS_FEATURE_HOLDING_REGISTERS) || defined(MBUS_FEATURE_INPUT_REGISTERS))) && (defined(MBUS_FEATURE_HOLDING_REGISTERS) && (defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII))))
/**
 * Queue a Write Multiple Registers (FC 0x10) request on a serial client.
 *
 * # Safety
 * `values` must be a valid non-null pointer.
 */
enum MbusStatusCode mbus_serial_write_multiple_registers(MbusClientId id,
                                                         uint16_t txn_id,
                                                         uint8_t unit_id,
                                                         uint16_t address,
                                                         const uint16_t *values,
                                                         uint16_t quantity);
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT) && (defined(MBUS_FEATURE_HOLDING_REGISTERS) || defined(MBUS_FEATURE_INPUT_REGISTERS))) && (defined(MBUS_FEATURE_HOLDING_REGISTERS) && defined(MBUS_FEATURE_NETWORK_TCP)))
/**
 * Queue a Read/Write Multiple Registers (FC 0x17) request.
 *
 * `write_values` must point to at least `write_qty` valid `u16` words.
 *
 * # Safety
 * `write_values` must be a valid non-null pointer.
 */
enum MbusStatusCode mbus_tcp_read_write_multiple_registers(MbusClientId id,
                                                           uint16_t txn_id,
                                                           uint8_t unit_id,
                                                           uint16_t read_address,
                                                           uint16_t read_qty,
                                                           uint16_t write_address,
                                                           const uint16_t *write_values,
                                                           uint16_t write_qty);
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_CLIENT) && (defined(MBUS_FEATURE_HOLDING_REGISTERS) || defined(MBUS_FEATURE_INPUT_REGISTERS))) && (defined(MBUS_FEATURE_HOLDING_REGISTERS) && (defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII))))
/**
 * Queue a Read/Write Multiple Registers (FC 0x17) request on a serial client.
 *
 * # Safety
 * `write_values` must be a valid non-null pointer.
 */
enum MbusStatusCode mbus_serial_read_write_multiple_registers(MbusClientId id,
                                                              uint16_t txn_id,
                                                              uint8_t unit_id,
                                                              uint16_t read_address,
                                                              uint16_t read_qty,
                                                              uint16_t write_address,
                                                              const uint16_t *write_values,
                                                              uint16_t write_qty);
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII))))
/**
 * Lock the global server pool (used only during server creation/destruction).
 */
extern void mbus_pool_lock(void);
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII))))
/**
 * Unlock the global server pool.
 */
extern void mbus_pool_unlock(void);
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII))))
/**
 * Lock a specific server instance.
 */
extern void mbus_server_lock(MbusServerId id);
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII))))
/**
 * Unlock a specific server instance.
 */
extern void mbus_server_unlock(MbusServerId id);
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII))) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII)))) && defined(MBUS_FEATURE_SERIAL_RTU))
/**
 * Creates a new Modbus Serial RTU server and returns an opaque server ID.
 *
 * # Parameters
 * - `transport` — Transport callbacks providing connect/disconnect/send/recv.
 *   The `recv` callback is responsible for t1.5/t3.5 silence detection (see
 *   the module-level timing section).
 * - `handlers`  — Application callback table. `NULL` slots automatically
 *   respond with `IllegalFunction` for that function code.
 * - `config`    — Server configuration (slave address, timeout).
 *
 * # Returns
 * A valid `MbusServerId` on success, or `MBUS_INVALID_SERVER_ID` on failure.
 *
 * `MBUS_INVALID_SERVER_ID` is returned when:
 * - Any pointer argument is `NULL`.
 * - Any required function pointer inside `transport` is `NULL`.
 * - `config.slave_address` is outside the valid range 1–247.
 * - The internal serial server pool is exhausted.
 *
 * # Safety
 * - `transport`, `handlers`, and `config` must remain valid for the entire
 *   lifetime of the server (until `mbus_serial_server_free` is called).
 * - All function pointers inside `transport` must be valid C functions.
 * - `handlers.userdata` must outlive the server.
 */
MbusServerId mbus_serial_rtu_server_new(const struct MbusTransportCallbacks *transport,
                                        const struct MbusServerHandlers *handlers,
                                        const struct MbusServerConfig *config);
#endif

#if (((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII))) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII)))) && defined(MBUS_FEATURE_SERIAL_ASCII))
/**
 * Creates a new Modbus Serial ASCII server and returns an opaque server ID.
 *
 * ASCII mode uses LRC checksums and `:` / CR-LF delimiters instead of CRC and
 * silence-based framing, so t1.5/t3.5 inter-character timing is relaxed.
 * The `recv` callback may still return `MbusErrFramingError` if the ASCII frame
 * is malformed (e.g. an unexpected character between `:` and CR-LF).
 *
 * # Parameters
 * - `transport` — Transport callbacks for the underlying serial port.
 * - `handlers`  — Application callback table.
 * - `config`    — Server configuration (slave address, timeout).
 *
 * # Returns
 * A valid `MbusServerId` on success, or `MBUS_INVALID_SERVER_ID` on failure.
 *
 * `MBUS_INVALID_SERVER_ID` is returned under the same conditions as
 * `mbus_serial_rtu_server_new` (null pointers, invalid slave address, pool full).
 *
 * # Safety
 * Same requirements as `mbus_serial_rtu_server_new`.
 */
MbusServerId mbus_serial_ascii_server_new(const struct MbusTransportCallbacks *transport,
                                          const struct MbusServerHandlers *handlers,
                                          const struct MbusServerConfig *config);
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII))) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII))))
/**
 * Destroys a Serial server and releases its pool slot.
 *
 * The server's transport is **not** automatically disconnected. Call
 * `mbus_serial_server_disconnect` first if the port may still be open.
 *
 * After this call, `id` becomes invalid and must not be reused.
 *
 * # Error conditions (no return value)
 * This function is infallible — an invalid or already-freed `id` is silently
 * ignored to simplify cleanup paths.
 */
void mbus_serial_server_free(MbusServerId id);
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII))) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII))))
/**
 * Opens the serial server's transport (e.g. opens and configures the UART).
 *
 * Invokes the `connect` function-pointer from `MbusTransportCallbacks`.
 *
 * # Returns
 * - `MbusOk` — Port opened successfully.
 * - `MbusErrConnectionFailed` — The `connect` callback returned a failure
 *   (e.g. the serial port path does not exist, or permission denied).
 * - `MbusErrIoError` — A lower-level I/O error occurred while configuring
 *   the port (baud rate, parity, stop bits).
 * - `MbusErrInvalidConfiguration` — The transport rejected the supplied
 *   configuration (e.g. unsupported baud rate on this hardware).
 * - `MbusErrInvalidClientId` — `id` is not a valid serial server slot.
 *
 * The server lock must be held while calling this function.
 */
enum MbusStatusCode mbus_serial_server_connect(MbusServerId id);
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII))) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII))))
/**
 * Closes the serial server's transport (e.g. releases the UART).
 *
 * Invokes the `disconnect` function-pointer from `MbusTransportCallbacks`.
 * Outstanding queued responses are discarded; the server pool slot remains
 * valid and can be reconnected with `mbus_serial_server_connect`.
 *
 * # Returns
 * - `MbusOk` — Port closed (or was already closed).
 * - `MbusErrInvalidClientId` — `id` is not a valid serial server slot.
 *
 * The server lock must be held while calling this function.
 */
enum MbusStatusCode mbus_serial_server_disconnect(MbusServerId id);
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII))) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII))))
/**
 * Drives the serial server state machine for one iteration.
 *
 * Must be called in a tight loop or cooperative scheduler task. Each call:
 * 1. **Response retry** — resends queued responses from previous failed sends.
 * 2. **Receive** — calls the `recv` transport callback once to collect bytes.
 * 3. **Frame assembly** — appends received bytes to the internal sliding window.
 * 4. **Dispatch** — when a complete RTU/ASCII frame is recognised, the matching
 *    `MbusServerHandlers` callback is invoked.
 * 5. **Send** — transmits the response via the `send` transport callback.
 *
 * # Returns
 * This function always returns `MbusOk` for normal poll-loop operation.
 * Transport-level events are handled internally:
 *
 * | `recv` result | Server action |
 * |---------------|---------------|
 * | `MbusOk` | Bytes appended to the receive buffer; frame assembly continues. |
 * | `MbusErrTimeout` | No data available this poll; silently skipped. |
 * | `MbusErrFramingError` | **Timing violation detected** (see below). Receive buffer is cleared; server resumes listening. |
 * | `MbusErrIoError` / `MbusErrConnectionClosed` | Transport is marked disconnected. Call `mbus_serial_server_connect` to reopen. |
 * | `send` fails | Response is queued for retry on subsequent polls (up to `max_send_retries`). |
 * | CRC/parse error | One byte is discarded and the sliding window re-syncs. |
 *
 * - `MbusErrInvalidClientId` — returned only when `id` is not a valid serial slot.
 *
 * # Framing error handling
 *
 * When the C `recv` callback detects a t1.5 inter-character gap violation (a
 * silence of 1.5 character times *inside* a frame), it must return
 * `MBUS_ERR_FRAMING_ERROR`.  The server will:
 * - Discard all bytes buffered so far for the current frame.
 * - **Not** disconnect — the bus is still usable.
 * - Resume waiting for the next valid frame on the next poll call.
 *
 * This is the correct behaviour per Modbus Serial Line Specification §2.5.1.2.
 *
 * The server lock must be held while calling this function.
 */
enum MbusStatusCode mbus_serial_server_poll(MbusServerId id);
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII))) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII))))
/**
 * Returns `true` if the serial server's transport is connected.
 *
 * Delegates to the `is_connected` function-pointer in `MbusTransportCallbacks`.
 * Returns `false` for an invalid or freed server ID.
 */
bool mbus_serial_server_is_connected(MbusServerId id);
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII))) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII))))
/**
 * Returns the number of requests waiting in the priority queue.
 *
 * For serial servers the queue depth is fixed at 1 (half-duplex bus allows
 * only one request in flight), so this will always be 0 or 1.
 * Returns `0` for an invalid or freed server ID.
 */
uintptr_t mbus_serial_server_pending_request_count(MbusServerId id);
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII))) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII))))
/**
 * Returns the number of responses waiting for retry (failed sends).
 *
 * A non-zero value means the `send` transport callback has failed at least once
 * and the server is holding the unsent frame for retry on the next poll.
 * Returns `0` for an invalid or freed server ID.
 */
uintptr_t mbus_serial_server_pending_response_count(MbusServerId id);
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII))) && (defined(MBUS_FEATURE_C_SERVER) && defined(MBUS_FEATURE_NETWORK_TCP)))
/**
 * Creates a new Modbus TCP server and returns an opaque server ID.
 *
 * # Parameters
 * - `transport` — Transport callbacks providing connect/disconnect/send/recv operations.
 * - `handlers`  — Application callback table.  Slots left `NULL` respond with
 *   `IllegalFunction` automatically.
 * - `config`    — Server configuration (slave address, timeouts).
 *
 * # Returns
 * A valid `MbusServerId` on success, or `MBUS_INVALID_SERVER_ID` on failure.
 *
 * `MBUS_INVALID_SERVER_ID` is returned when:
 * - Any pointer argument is `NULL`.
 * - Any required function pointer inside `transport` is `NULL`.
 * - `config.slave_address` is out of the valid Modbus range (1–247).
 * - The internal server pool is exhausted (all slots occupied).
 *
 * # Safety
 * - `transport`, `handlers`, and `config` must be non-null and remain valid for
 *   the entire lifetime of the server (until `mbus_tcp_server_free` is called).
 * - All function pointers inside `transport` must be valid C functions.
 * - `handlers.userdata` must outlive the server.
 */
MbusServerId mbus_tcp_server_new(const struct MbusTransportCallbacks *transport,
                                 const struct MbusServerHandlers *handlers,
                                 const struct MbusServerConfig *config);
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII))) && (defined(MBUS_FEATURE_C_SERVER) && defined(MBUS_FEATURE_NETWORK_TCP)))
/**
 * Destroys a TCP server and releases its pool slot.
 *
 * The server's transport is **not** automatically disconnected before freeing.
 * Call `mbus_tcp_server_disconnect` first if the transport may still be active.
 *
 * After this call, `id` becomes invalid and must not be reused.
 *
 * # Error conditions (no return value)
 * This function is infallible — an invalid or already-freed `id` is silently
 * ignored to simplify cleanup paths.
 *
 * # Safety
 * The caller is responsible for ensuring no other thread is using the server
 * (poll / connect / disconnect) concurrently when this is called.
 */
void mbus_tcp_server_free(MbusServerId id);
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII))) && (defined(MBUS_FEATURE_C_SERVER) && defined(MBUS_FEATURE_NETWORK_TCP)))
/**
 * Opens the server's transport (e.g. begins accepting TCP connections).
 *
 * Invokes the `connect` function-pointer from the `MbusTransportCallbacks` struct
 * supplied to `mbus_tcp_server_new`.
 *
 * # Returns
 * - `MbusOk` — Transport connected successfully.
 * - `MbusErrConnectionFailed` — The `connect` callback returned a failure.
 * - `MbusErrIoError` — A lower-level I/O error occurred in the callback.
 * - `MbusErrInvalidConfiguration` — The transport was already connected, or the
 *   configuration passed to the callback was rejected.
 * - `MbusErrInvalidClientId` — `id` does not refer to a live TCP server slot.
 *
 * The server lock must be held while calling this function.
 */
enum MbusStatusCode mbus_tcp_server_connect(MbusServerId id);
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII))) && (defined(MBUS_FEATURE_C_SERVER) && defined(MBUS_FEATURE_NETWORK_TCP)))
/**
 * Closes the server's transport.
 *
 * Invokes the `disconnect` function-pointer from `MbusTransportCallbacks`.
 * Outstanding queued responses are discarded; the server pool slot remains
 * valid and can be reconnected with `mbus_tcp_server_connect`.
 *
 * # Returns
 * - `MbusOk` — Transport disconnected (or was already disconnected).
 * - `MbusErrInvalidClientId` — `id` does not refer to a live TCP server slot.
 *
 * The server lock must be held while calling this function.
 */
enum MbusStatusCode mbus_tcp_server_disconnect(MbusServerId id);
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII))) && (defined(MBUS_FEATURE_C_SERVER) && defined(MBUS_FEATURE_NETWORK_TCP)))
/**
 * Drives the server state machine for one iteration.
 *
 * Must be called in a tight loop or cooperative event loop. Each call performs:
 * 1. **Response retry** — resends queued responses from previous failed sends.
 * 2. **Receive** — reads bytes from the transport `recv` callback.
 * 3. **Frame parse** — assembles complete Modbus MBAP + PDU frames.
 * 4. **Dispatch** — calls the matching `MbusServerHandlers` callback.
 * 5. **Send** — transmits the response over the transport `send` callback.
 *
 * # Returns
 * `mbus_tcp_server_poll` itself always returns `MbusOk`; individual transport
 * failures are handled internally by the server's resilience layer:
 *
 * | Internal event | Server behaviour |
 * |----------------|------------------|
 * | `recv` returns `Timeout` | No data this poll; move on silently. |
 * | `recv` returns `IoError` / `ConnectionClosed` | Transport is disconnected; server waits for reconnect. |
 * | `send` fails | Response is queued for retry on the next poll (up to `max_send_retries`). |
 * | Frame parse error | One byte is discarded and the sliding window re-syncs. |
 *
 * - `MbusErrInvalidClientId` — `id` does not refer to a live TCP server slot.
 *
 * The server lock must be held while calling this function.
 */
enum MbusStatusCode mbus_tcp_server_poll(MbusServerId id);
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII))) && (defined(MBUS_FEATURE_C_SERVER) && defined(MBUS_FEATURE_NETWORK_TCP)))
/**
 * Returns `true` if the server's transport reports itself as connected.
 *
 * Delegates to the `is_connected` function-pointer in `MbusTransportCallbacks`.
 * Returns `false` for an invalid or freed server ID.
 */
bool mbus_tcp_server_is_connected(MbusServerId id);
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII))) && (defined(MBUS_FEATURE_C_SERVER) && defined(MBUS_FEATURE_NETWORK_TCP)))
/**
 * Returns the number of requests currently waiting in the priority queue.
 *
 * Reflects how many parsed requests are buffered waiting to be dispatched
 * (only non-zero when `ResilienceConfig::enable_priority_queue` is active).
 * Returns `0` for an invalid or freed server ID.
 */
uintptr_t mbus_tcp_server_pending_request_count(MbusServerId id);
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && (defined(MBUS_FEATURE_C_SERVER) && (defined(MBUS_FEATURE_NETWORK_TCP) || defined(MBUS_FEATURE_SERIAL_RTU) || defined(MBUS_FEATURE_SERIAL_ASCII))) && (defined(MBUS_FEATURE_C_SERVER) && defined(MBUS_FEATURE_NETWORK_TCP)))
/**
 * Returns the number of responses waiting for retry (failed sends).
 *
 * A non-zero value means the `send` transport callback has failed at least
 * once and the server is holding the unsent frames for retry.
 * Returns `0` for an invalid or freed server ID.
 */
uintptr_t mbus_tcp_server_pending_response_count(MbusServerId id);
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_GATEWAY))
/**
 * Create a new TCP↔TCP gateway.
 *
 * `upstream` describes the C-provided upstream transport callbacks.
 * `events` may be `NULL` to disable observability callbacks.
 *
 * On success writes the new handle into `*out_id` and returns
 * [`MbusStatusCode::MbusOk`]. On failure leaves `*out_id` set to
 * [`MBUS_INVALID_GATEWAY_ID`].
 *
 * # Safety
 * `upstream` and `out_id` must be non-NULL. `events` may be NULL. All
 * non-NULL pointers must point to valid memory for the duration of this
 * call. The function pointers in `upstream` and `events` must outlive the
 * gateway instance.
 */
enum MbusStatusCode mbus_gateway_new(const struct MbusTransportCallbacks *upstream,
                                     const struct MbusGatewayCallbacks *events,
                                     MbusGatewayId *out_id);
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_GATEWAY))
/**
 * Register a TCP downstream channel.
 *
 * Channels are indexed in registration order starting at 0. The new index
 * is written to `*out_channel_idx`.
 *
 * # Safety
 * `callbacks` and `out_channel_idx` must be non-NULL and point to valid
 * memory for the duration of this call.
 */
enum MbusStatusCode mbus_gateway_add_downstream(MbusGatewayId id,
                                                const struct MbusTransportCallbacks *callbacks,
                                                uint16_t *out_channel_idx);
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_GATEWAY))
/**
 * Register an exact unit-ID → channel-index route.
 *
 * Returns [`MbusStatusCode::MbusErrInvalidConfiguration`] if the routing
 * table is full, the unit is already registered, or `channel_idx` is out
 * of range.
 */
enum MbusStatusCode mbus_gateway_add_unit_route(MbusGatewayId id,
                                                uint8_t unit_id,
                                                uint16_t channel_idx);
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_GATEWAY))
/**
 * Register a contiguous unit-ID range → channel-index route. `unit_min`
 * and `unit_max` are inclusive.
 */
enum MbusStatusCode mbus_gateway_add_range_route(MbusGatewayId id,
                                                 uint8_t unit_min,
                                                 uint8_t unit_max,
                                                 uint16_t channel_idx);
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_GATEWAY))
/**
 * Drive one poll cycle of the gateway. See
 * [`GatewayServices::poll`](mbus_gateway::GatewayServices::poll) for the
 * semantics.
 */
enum MbusStatusCode mbus_gateway_poll(MbusGatewayId id);
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_GATEWAY))
/**
 * Free the gateway at `id`. Returns [`MbusStatusCode::MbusOk`] on success
 * or [`MbusStatusCode::MbusErrInvalidClientId`] if the handle is invalid.
 */
enum MbusStatusCode mbus_gateway_free(MbusGatewayId id);
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_GATEWAY))
/**
 * Global pool lock — held only during create/destroy operations. Reuses
 * the same symbol exported by the client pool.
 */
extern void mbus_pool_lock(void);
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_GATEWAY))
/**
 * Release the global pool lock.
 */
extern void mbus_pool_unlock(void);
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_GATEWAY))
/**
 * Per-instance lock — held continuously during `mbus_gateway_poll`.
 */
extern void mbus_gateway_lock(MbusGatewayId id);
#endif

#if ((defined(MBUS_FEATURE_C_CLIENT) || defined(MBUS_FEATURE_C_SERVER) || defined(MBUS_FEATURE_DOTNET) || defined(MBUS_FEATURE_GO)) && defined(MBUS_FEATURE_C_GATEWAY))
/**
 * Release a per-instance gateway lock.
 */
extern void mbus_gateway_unlock(MbusGatewayId id);
#endif

#if (defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32))
/**
 * Creates a new RTU serial client.
 *
 * `port` must be a NUL-terminated UTF-8 path string (e.g. `"/dev/ttyUSB0"` on
 * Linux or `"COM3"` on Windows).  `parity` is `0`=None, `1`=Even, `2`=Odd.
 * Returns `null` on any configuration or construction failure.
 *
 * # Safety
 *
 * `port` must point to a valid NUL-terminated string for the duration of the call.
 */
struct MbusDnSerialClient *mbus_dn_serial_client_new_rtu(const char *port,
                                                         uint32_t baud_rate,
                                                         uint8_t data_bits,
                                                         uint8_t parity,
                                                         uint8_t stop_bits,
                                                         uint32_t response_timeout_ms);
#endif

#if (defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32))
/**
 * Creates a new ASCII serial client.
 *
 * See [`mbus_dn_serial_client_new_rtu`] for parameter semantics.
 *
 * # Safety
 *
 * `port` must point to a valid NUL-terminated string for the duration of the call.
 */
struct MbusDnSerialClient *mbus_dn_serial_client_new_ascii(const char *port,
                                                           uint32_t baud_rate,
                                                           uint8_t data_bits,
                                                           uint8_t parity,
                                                           uint8_t stop_bits,
                                                           uint32_t response_timeout_ms);
#endif

#if (defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32))
/**
 * Destroys the serial client and releases all associated resources.
 *
 * # Safety
 *
 * `handle` must be a valid pointer originally returned by one of the
 * `mbus_dn_serial_client_new_*` constructors.  Calling this function more
 * than once with the same pointer is undefined behaviour.
 */
void mbus_dn_serial_client_free(struct MbusDnSerialClient *handle);
#endif

#if (defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32))
/**
 * Opens the underlying serial port.
 *
 * # Safety
 *
 * `handle` must be a valid client pointer.
 */
MbusDnStatus mbus_dn_serial_client_connect(struct MbusDnSerialClient *handle);
#endif

#if (defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32))
/**
 * Closes the underlying serial port.
 *
 * # Safety
 *
 * `handle` must be a valid client pointer.
 */
MbusDnStatus mbus_dn_serial_client_disconnect(struct MbusDnSerialClient *handle);
#endif

#if (defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32))
/**
 * Sets the per-request timeout in milliseconds (`0` = no timeout).
 *
 * # Safety
 *
 * `handle` must be a valid client pointer.
 */
void mbus_dn_serial_client_set_request_timeout_ms(struct MbusDnSerialClient *handle,
                                                  uint64_t timeout_ms);
#endif

#if (defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32))
/**
 * Reads `quantity` holding registers (FC03).
 *
 * # Safety
 *
 * `handle`, `out_buf`, and `out_count` must be valid non-null pointers.
 */
MbusDnStatus mbus_dn_serial_client_read_holding_registers(struct MbusDnSerialClient *handle,
                                                          uint8_t unit_id,
                                                          uint16_t address,
                                                          uint16_t quantity,
                                                          uint16_t *out_buf,
                                                          uint16_t out_buf_len,
                                                          uint16_t *out_count);
#endif

#if (defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32))
/**
 * Writes a single holding register (FC06).
 *
 * # Safety
 *
 * `handle` must be a valid client pointer.
 */
MbusDnStatus mbus_dn_serial_client_write_single_register(struct MbusDnSerialClient *handle,
                                                         uint8_t unit_id,
                                                         uint16_t address,
                                                         uint16_t value,
                                                         uint16_t *out_address,
                                                         uint16_t *out_value);
#endif

#if (defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32))
/**
 * Writes multiple holding registers (FC16).
 *
 * `values` is a slice of `quantity` big-endian register words.
 *
 * # Safety
 *
 * `handle` and `values` must be valid non-null pointers.
 */
MbusDnStatus mbus_dn_serial_client_write_multiple_registers(struct MbusDnSerialClient *handle,
                                                            uint8_t unit_id,
                                                            uint16_t address,
                                                            const uint16_t *values,
                                                            uint16_t quantity,
                                                            uint16_t *out_address,
                                                            uint16_t *out_quantity);
#endif

#if (defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32))
/**
 * Reads `quantity` input registers (FC04).
 *
 * # Safety
 *
 * `handle`, `out_buf`, and `out_count` must be valid non-null pointers.
 */
MbusDnStatus mbus_dn_serial_client_read_input_registers(struct MbusDnSerialClient *handle,
                                                        uint8_t unit_id,
                                                        uint16_t address,
                                                        uint16_t quantity,
                                                        uint16_t *out_buf,
                                                        uint16_t out_buf_len,
                                                        uint16_t *out_count);
#endif

#if (defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32))
/**
 * Applies an AND/OR bitmask to a holding register (FC22).
 *
 * # Safety
 *
 * `handle` must be a valid client pointer.
 */
MbusDnStatus mbus_dn_serial_client_mask_write_register(struct MbusDnSerialClient *handle,
                                                       uint8_t unit_id,
                                                       uint16_t address,
                                                       uint16_t and_mask,
                                                       uint16_t or_mask);
#endif

#if (defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32))
/**
 * Reads and simultaneously writes multiple holding registers (FC23).
 *
 * # Safety
 *
 * `handle`, `write_values`, `out_buf`, and `out_count` must be valid non-null pointers.
 */
MbusDnStatus mbus_dn_serial_client_read_write_multiple_registers(struct MbusDnSerialClient *handle,
                                                                 uint8_t unit_id,
                                                                 uint16_t read_address,
                                                                 uint16_t read_quantity,
                                                                 uint16_t write_address,
                                                                 const uint16_t *write_values,
                                                                 uint16_t write_quantity,
                                                                 uint16_t *out_buf,
                                                                 uint16_t out_buf_len,
                                                                 uint16_t *out_count);
#endif

#if ((defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32)) && defined(MBUS_FEATURE_COILS))
/**
 * Reads `quantity` coils (FC01).
 *
 * # Safety
 *
 * `handle`, `out_buf`, and `out_count` must be valid non-null pointers.
 */
MbusDnStatus mbus_dn_serial_client_read_coils(struct MbusDnSerialClient *handle,
                                              uint8_t unit_id,
                                              uint16_t address,
                                              uint16_t quantity,
                                              uint8_t *out_buf,
                                              uint16_t out_buf_len,
                                              uint16_t *out_count);
#endif

#if ((defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32)) && defined(MBUS_FEATURE_COILS))
/**
 * Writes a single coil (FC05).
 *
 * # Safety
 *
 * `handle` must be a valid client pointer.
 */
MbusDnStatus mbus_dn_serial_client_write_single_coil(struct MbusDnSerialClient *handle,
                                                     uint8_t unit_id,
                                                     uint16_t address,
                                                     uint8_t value,
                                                     uint16_t *out_address,
                                                     uint8_t *out_value);
#endif

#if ((defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32)) && defined(MBUS_FEATURE_COILS))
/**
 * Writes multiple coils (FC15).
 *
 * # Safety
 *
 * `handle` and `packed_coils` must be valid non-null pointers.
 */
MbusDnStatus mbus_dn_serial_client_write_multiple_coils(struct MbusDnSerialClient *handle,
                                                        uint8_t unit_id,
                                                        uint16_t address,
                                                        const uint8_t *packed_coils,
                                                        uint16_t byte_count,
                                                        uint16_t coil_count,
                                                        uint16_t *out_address,
                                                        uint16_t *out_quantity);
#endif

#if ((defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32)) && defined(MBUS_FEATURE_DISCRETE_INPUTS))
/**
 * Reads `quantity` discrete inputs (FC02).
 *
 * # Safety
 *
 * `handle`, `out_buf`, and `out_count` must be valid non-null pointers.
 */
MbusDnStatus mbus_dn_serial_client_read_discrete_inputs(struct MbusDnSerialClient *handle,
                                                        uint8_t unit_id,
                                                        uint16_t address,
                                                        uint16_t quantity,
                                                        uint8_t *out_buf,
                                                        uint16_t out_buf_len,
                                                        uint16_t *out_count);
#endif

#if ((defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32)) && defined(MBUS_FEATURE_FIFO))
/**
 * Reads the FIFO queue (FC24).
 *
 * # Safety
 *
 * `handle`, `out_buf`, and `out_count` must be valid non-null pointers.
 */
MbusDnStatus mbus_dn_serial_client_read_fifo_queue(struct MbusDnSerialClient *handle,
                                                   uint8_t unit_id,
                                                   uint16_t address,
                                                   uint16_t *out_buf,
                                                   uint16_t out_buf_len,
                                                   uint16_t *out_count);
#endif

#if ((defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32)) && defined(MBUS_FEATURE_DIAGNOSTICS))
/**
 * Reads the device exception status (FC07).
 *
 * # Safety
 *
 * `handle` and `out_status` must be valid non-null pointers.
 */
MbusDnStatus mbus_dn_serial_client_read_exception_status(struct MbusDnSerialClient *handle,
                                                         uint8_t unit_id,
                                                         uint8_t *out_status);
#endif

#if ((defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32)) && defined(MBUS_FEATURE_DIAGNOSTICS))
/**
 * Reads the communication event counter (FC11).
 *
 * # Safety
 *
 * `handle`, `out_status`, and `out_event_count` must be valid non-null pointers.
 */
MbusDnStatus mbus_dn_serial_client_get_comm_event_counter(struct MbusDnSerialClient *handle,
                                                          uint8_t unit_id,
                                                          uint16_t *out_status,
                                                          uint16_t *out_event_count);
#endif

#if ((defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32)) && defined(MBUS_FEATURE_DIAGNOSTICS))
/**
 * Reads the communication event log (FC12).
 *
 * # Safety
 *
 * `handle`, `out_buf`, and `out_count` must be valid non-null pointers.
 */
MbusDnStatus mbus_dn_serial_client_get_comm_event_log(struct MbusDnSerialClient *handle,
                                                      uint8_t unit_id,
                                                      uint8_t *out_buf,
                                                      uint16_t out_buf_len,
                                                      uint16_t *out_count);
#endif

#if ((defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32)) && defined(MBUS_FEATURE_DIAGNOSTICS))
/**
 * Reports the server identifier (FC17).
 *
 * # Safety
 *
 * `handle`, `out_buf`, and `out_count` must be valid non-null pointers.
 */
MbusDnStatus mbus_dn_serial_client_report_server_id(struct MbusDnSerialClient *handle,
                                                    uint8_t unit_id,
                                                    uint8_t *out_buf,
                                                    uint16_t out_buf_len,
                                                    uint16_t *out_count);
#endif

#if ((defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32)) && defined(MBUS_FEATURE_DIAGNOSTICS))
/**
 * Sends a Diagnostics (FC08) request.
 *
 * # Safety
 *
 * `handle`, `out_sub_function`, `out_buf`, and `out_count` must be valid non-null
 * pointers. If `data_in_count > 0`, `data_in` must point to at least `data_in_count`
 * valid `u16` words.
 */
MbusDnStatus mbus_dn_serial_client_diagnostics(struct MbusDnSerialClient *handle,
                                               uint8_t unit_id,
                                               uint16_t sub_function,
                                               const uint16_t *data_in,
                                               uint16_t data_in_count,
                                               uint16_t *out_sub_function,
                                               uint16_t *out_buf,
                                               uint16_t out_buf_len,
                                               uint16_t *out_count);
#endif

#if ((defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32)) && defined(MBUS_FEATURE_FILE_RECORD))
/**
 * Reads one or more file records (FC14) over a serial connection.
 *
 * # Safety
 *
 * `handle`, `sub_reqs`, `out_buf`, and `out_count` must be valid non-null pointers.
 * `sub_reqs` must be valid for `sub_req_count` items.
 */
MbusDnStatus mbus_dn_serial_client_read_file_record(struct MbusDnSerialClient *handle,
                                                    uint8_t unit_id,
                                                    const struct MbusDnSerialSubRequest *sub_reqs,
                                                    uint16_t sub_req_count,
                                                    uint16_t *out_buf,
                                                    uint16_t out_buf_len,
                                                    uint16_t *out_count);
#endif

#if ((defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32)) && defined(MBUS_FEATURE_FILE_RECORD))
/**
 * Writes one or more file records (FC15) over a serial connection.
 *
 * # Safety
 *
 * `handle` and `sub_reqs` must be valid non-null pointers. `sub_reqs` must be valid
 * for `sub_req_count` items. For each item, `data` must be valid for `data_len` words.
 */
MbusDnStatus mbus_dn_serial_client_write_file_record(struct MbusDnSerialClient *handle,
                                                     uint8_t unit_id,
                                                     const struct MbusDnSerialSubRequest *sub_reqs,
                                                     uint16_t sub_req_count);
#endif

#if (defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32))
/**
 * Creates a new async TCP client targeting `host:port`.
 *
 * `host` must be a NUL-terminated UTF-8 string.  The returned pointer
 * must eventually be released with [`mbus_dn_tcp_client_free`].  Returns
 * `null` if `host` is null, not valid UTF-8, or the underlying constructor
 * fails (for example because no Tokio runtime could be started).
 *
 * # Safety
 *
 * `host` must point to a valid NUL-terminated string for the duration of
 * this call.
 */
struct MbusDnTcpClient *mbus_dn_tcp_client_new(const char *host, uint16_t port);
#endif

#if (defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32))
/**
 * Releases an `MbusDnTcpClient` previously returned from
 * [`mbus_dn_tcp_client_new`].
 *
 * Drops the underlying `AsyncTcpClient`, which signals its background
 * Tokio task to exit.  No-op if `handle` is null.  Safe to call exactly
 * once per handle.
 *
 * # Safety
 *
 * `handle` must be either null or a pointer previously returned from
 * [`mbus_dn_tcp_client_new`] that has not already been freed.
 */
void mbus_dn_tcp_client_free(struct MbusDnTcpClient *handle);
#endif

#if (defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32))
/**
 * Establishes the TCP transport connection.
 *
 * Blocks the calling thread until the connection completes or fails.
 *
 * # Safety
 *
 * `handle` must be a non-null pointer previously returned from
 * [`mbus_dn_tcp_client_new`] and still alive.
 */
MbusDnStatus mbus_dn_tcp_client_connect(struct MbusDnTcpClient *handle);
#endif

#if (defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32))
/**
 * Closes the TCP transport gracefully.
 *
 * Drains any in-flight or queued requests with `ConnectionClosed`.  After
 * this call the client can be reconnected with
 * [`mbus_dn_tcp_client_connect`].
 *
 * # Safety
 *
 * See [`mbus_dn_tcp_client_connect`].
 */
MbusDnStatus mbus_dn_tcp_client_disconnect(struct MbusDnTcpClient *handle);
#endif

#if (defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32))
/**
 * Sets a per-request timeout in milliseconds; `0` disables the timeout.
 *
 * # Safety
 *
 * See [`mbus_dn_tcp_client_connect`].
 */
MbusDnStatus mbus_dn_tcp_client_set_request_timeout_ms(struct MbusDnTcpClient *handle,
                                                       uint64_t timeout_ms);
#endif

#if (defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32))
/**
 * Returns `1` when there are requests in flight awaiting a response, `0`
 * otherwise; returns `0` when `handle` is null.
 *
 * # Safety
 *
 * See [`mbus_dn_tcp_client_connect`].
 */
uint8_t mbus_dn_tcp_client_has_pending_requests(struct MbusDnTcpClient *handle);
#endif

#if (defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32))
/**
 * Reads `quantity` holding registers (FC03) starting at `address` from
 * the given `unit_id` and copies them, in declaration order, into the
 * caller-supplied `out_buf` of length `out_buf_len` (in `u16` elements).
 *
 * On success writes the number of registers actually read into
 * `out_count` (which equals `quantity`) and returns `MbusOk`.
 *
 * Returns `MbusErrBufferTooSmall` if `out_buf_len < quantity`.
 *
 * # Safety
 *
 * * `handle` must be a valid client pointer.
 * * `out_buf` must point to writable storage for at least `out_buf_len`
 *   `u16` values.
 * * `out_count` must point to a writable `u16`.
 */
MbusDnStatus mbus_dn_tcp_client_read_holding_registers(struct MbusDnTcpClient *handle,
                                                       uint8_t unit_id,
                                                       uint16_t address,
                                                       uint16_t quantity,
                                                       uint16_t *out_buf,
                                                       uint16_t out_buf_len,
                                                       uint16_t *out_count);
#endif

#if (defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32))
/**
 * Writes a single holding register (FC06).
 *
 * On success, writes the echoed `(address, value)` to `out_address` and
 * `out_value` if those pointers are non-null.
 *
 * # Safety
 *
 * * `handle` must be a valid client pointer.
 * * `out_address` and `out_value` may be null; if non-null they must
 *   point to writable `u16` storage.
 */
MbusDnStatus mbus_dn_tcp_client_write_single_register(struct MbusDnTcpClient *handle,
                                                      uint8_t unit_id,
                                                      uint16_t address,
                                                      uint16_t value,
                                                      uint16_t *out_address,
                                                      uint16_t *out_value);
#endif

#if (defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32))
/**
 * Writes `quantity` holding registers (FC16) starting at `address`.
 *
 * `values` must point to `quantity` `u16` values in declaration order.
 *
 * On success, writes the echoed `(starting_address, quantity)` to
 * `out_address` and `out_quantity` if non-null.
 *
 * # Safety
 *
 * * `handle` must be a valid client pointer.
 * * `values` must point to at least `quantity` readable `u16` values.
 * * `out_address` and `out_quantity` may be null.
 */
MbusDnStatus mbus_dn_tcp_client_write_multiple_registers(struct MbusDnTcpClient *handle,
                                                         uint8_t unit_id,
                                                         uint16_t address,
                                                         const uint16_t *values,
                                                         uint16_t quantity,
                                                         uint16_t *out_address,
                                                         uint16_t *out_quantity);
#endif

#if ((defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32)) && defined(MBUS_FEATURE_COILS))
/**
 * Reads `quantity` coils (FC01) starting at `address` from `unit_id`.
 *
 * Writes bit-packed coil bytes (Modbus wire format, LSB-first) into `out_buf`.
 * `out_buf_len` is the buffer length in bytes; must be ≥ `ceil(quantity / 8)`.
 * On success writes the byte count to `out_count`.
 *
 * # Safety
 *
 * `handle`, `out_buf`, and `out_count` must be valid non-null pointers.
 */
MbusDnStatus mbus_dn_tcp_client_read_coils(struct MbusDnTcpClient *handle,
                                           uint8_t unit_id,
                                           uint16_t address,
                                           uint16_t quantity,
                                           uint8_t *out_buf,
                                           uint16_t out_buf_len,
                                           uint16_t *out_count);
#endif

#if ((defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32)) && defined(MBUS_FEATURE_COILS))
/**
 * Writes a single coil (FC05). `value != 0` means ON.
 *
 * On success, writes echoed address to `out_address` and value (`1`=ON, `0`=OFF)
 * to `out_value` if those pointers are non-null.
 *
 * # Safety
 *
 * `handle` must be a valid client pointer.
 */
MbusDnStatus mbus_dn_tcp_client_write_single_coil(struct MbusDnTcpClient *handle,
                                                  uint8_t unit_id,
                                                  uint16_t address,
                                                  uint8_t value,
                                                  uint16_t *out_address,
                                                  uint8_t *out_value);
#endif

#if ((defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32)) && defined(MBUS_FEATURE_COILS))
/**
 * Writes multiple coils (FC15) starting at `address`.
 *
 * `packed_coils` is Modbus bit-packed coil data (LSB-first). `byte_count` is
 * the number of bytes in `packed_coils`; `coil_count` is the number of coils.
 *
 * # Safety
 *
 * `handle` and `packed_coils` must be valid non-null pointers.
 */
MbusDnStatus mbus_dn_tcp_client_write_multiple_coils(struct MbusDnTcpClient *handle,
                                                     uint8_t unit_id,
                                                     uint16_t address,
                                                     const uint8_t *packed_coils,
                                                     uint16_t byte_count,
                                                     uint16_t coil_count,
                                                     uint16_t *out_address,
                                                     uint16_t *out_quantity);
#endif

#if ((defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32)) && defined(MBUS_FEATURE_DISCRETE_INPUTS))
/**
 * Reads `quantity` discrete inputs (FC02) starting at `address` from `unit_id`.
 *
 * Writes bit-packed bytes into `out_buf`. On success writes byte count to `out_count`.
 *
 * # Safety
 *
 * `handle`, `out_buf`, and `out_count` must be valid non-null pointers.
 */
MbusDnStatus mbus_dn_tcp_client_read_discrete_inputs(struct MbusDnTcpClient *handle,
                                                     uint8_t unit_id,
                                                     uint16_t address,
                                                     uint16_t quantity,
                                                     uint8_t *out_buf,
                                                     uint16_t out_buf_len,
                                                     uint16_t *out_count);
#endif

#if (defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32))
/**
 * Reads `quantity` input registers (FC04) starting at `address` from `unit_id`.
 *
 * # Safety
 *
 * `handle`, `out_buf`, and `out_count` must be valid non-null pointers.
 */
MbusDnStatus mbus_dn_tcp_client_read_input_registers(struct MbusDnTcpClient *handle,
                                                     uint8_t unit_id,
                                                     uint16_t address,
                                                     uint16_t quantity,
                                                     uint16_t *out_buf,
                                                     uint16_t out_buf_len,
                                                     uint16_t *out_count);
#endif

#if (defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32))
/**
 * Applies an AND/OR bitmask to a holding register (FC22).
 *
 * # Safety
 *
 * `handle` must be a valid client pointer.
 */
MbusDnStatus mbus_dn_tcp_client_mask_write_register(struct MbusDnTcpClient *handle,
                                                    uint8_t unit_id,
                                                    uint16_t address,
                                                    uint16_t and_mask,
                                                    uint16_t or_mask);
#endif

#if (defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32))
/**
 * Reads and simultaneously writes multiple holding registers (FC23).
 *
 * # Safety
 *
 * `handle`, `write_values`, `out_buf`, and `out_count` must be valid non-null pointers.
 */
MbusDnStatus mbus_dn_tcp_client_read_write_multiple_registers(struct MbusDnTcpClient *handle,
                                                              uint8_t unit_id,
                                                              uint16_t read_address,
                                                              uint16_t read_quantity,
                                                              uint16_t write_address,
                                                              const uint16_t *write_values,
                                                              uint16_t write_quantity,
                                                              uint16_t *out_buf,
                                                              uint16_t out_buf_len,
                                                              uint16_t *out_count);
#endif

#if ((defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32)) && defined(MBUS_FEATURE_FIFO))
/**
 * Reads the FIFO queue (FC24) at `address` from `unit_id`.
 *
 * # Safety
 *
 * `handle`, `out_buf`, and `out_count` must be valid non-null pointers.
 */
MbusDnStatus mbus_dn_tcp_client_read_fifo_queue(struct MbusDnTcpClient *handle,
                                                uint8_t unit_id,
                                                uint16_t address,
                                                uint16_t *out_buf,
                                                uint16_t out_buf_len,
                                                uint16_t *out_count);
#endif

#if ((defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32)) && defined(MBUS_FEATURE_DIAGNOSTICS))
/**
 * Reads the device exception status (FC07).
 *
 * # Safety
 *
 * `handle` and `out_status` must be valid non-null pointers.
 */
MbusDnStatus mbus_dn_tcp_client_read_exception_status(struct MbusDnTcpClient *handle,
                                                      uint8_t unit_id,
                                                      uint8_t *out_status);
#endif

#if ((defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32)) && defined(MBUS_FEATURE_DIAGNOSTICS))
/**
 * Reads the communication event counter (FC11).
 *
 * On success writes `status_word` and `event_count` to the respective out pointers.
 *
 * # Safety
 *
 * `handle`, `out_status`, and `out_event_count` must be valid non-null pointers.
 */
MbusDnStatus mbus_dn_tcp_client_get_comm_event_counter(struct MbusDnTcpClient *handle,
                                                       uint8_t unit_id,
                                                       uint16_t *out_status,
                                                       uint16_t *out_event_count);
#endif

#if ((defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32)) && defined(MBUS_FEATURE_DIAGNOSTICS))
/**
 * Reads the communication event log (FC12).
 *
 * Writes the raw event bytes into `out_buf`. On success writes byte count to `out_count`.
 *
 * # Safety
 *
 * `handle`, `out_buf`, and `out_count` must be valid non-null pointers.
 */
MbusDnStatus mbus_dn_tcp_client_get_comm_event_log(struct MbusDnTcpClient *handle,
                                                   uint8_t unit_id,
                                                   uint8_t *out_buf,
                                                   uint16_t out_buf_len,
                                                   uint16_t *out_count);
#endif

#if ((defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32)) && defined(MBUS_FEATURE_DIAGNOSTICS))
/**
 * Reports the server identifier (FC17).
 *
 * Writes the raw server ID bytes into `out_buf`. On success writes byte count to `out_count`.
 *
 * # Safety
 *
 * `handle`, `out_buf`, and `out_count` must be valid non-null pointers.
 */
MbusDnStatus mbus_dn_tcp_client_report_server_id(struct MbusDnTcpClient *handle,
                                                 uint8_t unit_id,
                                                 uint8_t *out_buf,
                                                 uint16_t out_buf_len,
                                                 uint16_t *out_count);
#endif

#if ((defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32)) && defined(MBUS_FEATURE_DIAGNOSTICS))
/**
 * Sends a Diagnostics (FC08) request.
 *
 * `sub_function` is the 16-bit sub-function code (see Modbus spec table).
 * `data_in` / `data_in_count` form the optional request data words (`null` / `0` for
 * sub-functions that carry no data such as *Return Query Data* with zero words).
 *
 * On success writes the echoed sub-function code to `out_sub_function` and the
 * echoed data words into `out_buf`, then sets `out_count` to the number of words written.
 *
 * # Safety
 *
 * `handle`, `out_sub_function`, `out_buf`, and `out_count` must be valid non-null pointers.
 * If `data_in_count > 0`, `data_in` must point to at least `data_in_count` valid `u16` words.
 */
MbusDnStatus mbus_dn_tcp_client_diagnostics(struct MbusDnTcpClient *handle,
                                            uint8_t unit_id,
                                            uint16_t sub_function,
                                            const uint16_t *data_in,
                                            uint16_t data_in_count,
                                            uint16_t *out_sub_function,
                                            uint16_t *out_buf,
                                            uint16_t out_buf_len,
                                            uint16_t *out_count);
#endif

#if ((defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32)) && defined(MBUS_FEATURE_FILE_RECORD))
/**
 * Reads one or more file records (FC14).
 *
 * `sub_reqs` must point to `sub_req_count` [`MbusDnSubRequest`] descriptors in read mode
 * (i.e. each `data` field is `null` and `data_len` is `0`).
 *
 * On success the register words for all sub-requests are written contiguously into
 * `out_buf` (in the order of the sub-requests), and `out_count` is set to the total
 * number of words written.  The caller is expected to know the `record_length` of each
 * sub-request so it can split the flat buffer into per-record slices.
 *
 * # Safety
 *
 * `handle`, `sub_reqs`, `out_buf`, and `out_count` must be valid non-null pointers.
 * `sub_reqs` must be valid for `sub_req_count` items.
 */
MbusDnStatus mbus_dn_tcp_client_read_file_record(struct MbusDnTcpClient *handle,
                                                 uint8_t unit_id,
                                                 const struct MbusDnSubRequest *sub_reqs,
                                                 uint16_t sub_req_count,
                                                 uint16_t *out_buf,
                                                 uint16_t out_buf_len,
                                                 uint16_t *out_count);
#endif

#if ((defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32)) && defined(MBUS_FEATURE_FILE_RECORD))
/**
 * Writes one or more file records (FC15).
 *
 * `sub_reqs` must point to `sub_req_count` [`MbusDnSubRequest`] descriptors in write mode
 * (i.e. each `data` field points to `data_len` valid `u16` words, and `record_length == data_len`).
 *
 * Returns `MbusOk` when all records have been written successfully.
 *
 * # Safety
 *
 * `handle` and `sub_reqs` must be valid non-null pointers.  `sub_reqs` must be valid for
 * `sub_req_count` items.  For each item, `data` must be valid for `data_len` words.
 */
MbusDnStatus mbus_dn_tcp_client_write_file_record(struct MbusDnTcpClient *handle,
                                                  uint8_t unit_id,
                                                  const struct MbusDnSubRequest *sub_reqs,
                                                  uint16_t sub_req_count);
#endif

#if (defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32))
uintptr_t mbus_dn_tcp_client_handle_size(void);
#endif

#if (defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32))
/**
 * Creates a new gateway handle.
 *
 * `host` is the NUL-terminated upstream bind address (e.g. `"0.0.0.0"`).
 * Returns `null` if `host` is null or not valid UTF-8.
 *
 * # Safety
 *
 * `host` must point to a valid NUL-terminated string.
 */
struct MbusDnTcpGateway *mbus_dn_tcp_gateway_new(const char *host, uint16_t port);
#endif

#if (defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32))
/**
 * Destroys the gateway handle.
 *
 * Call [`mbus_dn_tcp_gateway_stop`] first if the gateway is running.
 *
 * # Safety
 *
 * `handle` must be a valid pointer returned by [`mbus_dn_tcp_gateway_new`].
 */
void mbus_dn_tcp_gateway_free(struct MbusDnTcpGateway *handle);
#endif

#if (defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32))
/**
 * Adds a TCP downstream and returns its zero-based channel index.
 *
 * Returns `u32::MAX` if `host` is null.
 *
 * # Safety
 *
 * `handle` and `host` must be valid non-null pointers.
 */
uint32_t mbus_dn_tcp_gateway_add_downstream(struct MbusDnTcpGateway *handle,
                                            const char *host,
                                            uint16_t port);
#endif

#if (defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32))
/**
 * Routes a single unit ID to a downstream channel.
 *
 * Returns `MbusOk` on success, `MbusErrInvalidAddress` if `unit_id == 0` or
 * `channel >= registered_downstream_count`.
 *
 * # Safety
 *
 * `handle` must be a valid gateway pointer.
 */
MbusDnStatus mbus_dn_tcp_gateway_add_unit_route(struct MbusDnTcpGateway *handle,
                                                uint8_t unit_id,
                                                uint32_t channel);
#endif

#if (defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32))
/**
 * Routes an inclusive unit-ID range `[min, max]` to a downstream channel.
 *
 * Returns `MbusErrInvalidAddress` if `min == 0`, `min > max`, or
 * `channel >= registered_downstream_count`.
 *
 * # Safety
 *
 * `handle` must be a valid gateway pointer.
 */
MbusDnStatus mbus_dn_tcp_gateway_add_range_route(struct MbusDnTcpGateway *handle,
                                                 uint8_t unit_min,
                                                 uint8_t unit_max,
                                                 uint32_t channel);
#endif

#if (defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32))
/**
 * Starts the gateway on a new background OS thread.
 *
 * Connects all registered downstreams and begins forwarding requests.
 * Returns `MbusOk` immediately; the thread runs until
 * [`mbus_dn_tcp_gateway_stop`] is called.
 *
 * Returns `MbusErrInvalidAddress` if no downstreams or no routes have been
 * registered.
 *
 * # Safety
 *
 * `handle` must be a valid gateway pointer.
 */
MbusDnStatus mbus_dn_tcp_gateway_start(struct MbusDnTcpGateway *handle);
#endif

#if (defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32))
/**
 * Signals the running gateway to stop.
 *
 * # Safety
 *
 * `handle` must be a valid gateway pointer.
 */
void mbus_dn_tcp_gateway_stop(struct MbusDnTcpGateway *handle);
#endif

#if (defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32))
/**
 * Creates a new TCP server handle.
 *
 * `host` is a NUL-terminated bind address (e.g. `"0.0.0.0"`).
 * `unit_id` is the Modbus unit ID to respond to.
 * `vtable` must remain valid for the lifetime of the server handle.
 *
 * Returns `null` if `host` or `vtable` is null, or if `unit_id` is invalid.
 *
 * # Safety
 *
 * * `host` must point to a valid NUL-terminated UTF-8 string.
 * * `vtable` must be non-null and the struct it points to must remain valid
 *   until [`mbus_dn_tcp_server_free`] is called.
 */
struct MbusDnTcpServer *mbus_dn_tcp_server_new(const char *host,
                                               uint16_t port,
                                               uint8_t unit_id,
                                               const struct MbusDnServerVtable *vtable);
#endif

#if (defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32))
/**
 * Destroys the server handle.
 *
 * Does **not** stop a running server — call [`mbus_dn_tcp_server_stop`]
 * first, wait for the background thread to exit, then call this.
 *
 * # Safety
 *
 * `handle` must be a valid pointer returned by [`mbus_dn_tcp_server_new`].
 * Calling this more than once is undefined behaviour.
 */
void mbus_dn_tcp_server_free(struct MbusDnTcpServer *handle);
#endif

#if (defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32))
/**
 * Starts the server on a new background OS thread.
 *
 * The thread blocks on `serve_with_shutdown` until [`mbus_dn_tcp_server_stop`]
 * is called or a fatal error occurs.  Returns immediately to the caller.
 *
 * # Safety
 *
 * `handle` must be a valid server pointer.
 */
MbusDnStatus mbus_dn_tcp_server_start(struct MbusDnTcpServer *handle);
#endif

#if (defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32))
/**
 * Signals the running server to stop.
 *
 * The background thread will finish in-flight sessions and then exit.
 *
 * # Safety
 *
 * `handle` must be a valid server pointer.
 */
void mbus_dn_tcp_server_stop(struct MbusDnTcpServer *handle);
#endif

#if (defined(MBUS_FEATURE_DOTNET) && !defined(MBUS_TARGET_WASM32))
/**
 * Returns a static C string describing `status`.
 *
 * Equivalent to [`crate::c::error::mbus_status_str`] but exported with a
 * `mbus_dn_` prefix so that it appears in the .NET-only header.
 *
 * The returned pointer is always valid (points to a static string literal).
 * The caller must NOT free it.
 */
const char *mbus_dn_status_str(MbusDnStatus status);
#endif

#if (defined(MBUS_FEATURE_GO) && !defined(MBUS_TARGET_WASM32))
/**
 * Creates a new RTU serial client.
 *
 * `port` must be a NUL-terminated UTF-8 path string (e.g. `"/dev/ttyUSB0"` on
 * Linux or `"COM3"` on Windows).  `parity` is `0`=None, `1`=Even, `2`=Odd.
 * Returns `null` on any configuration or construction failure.
 *
 * # Safety
 *
 * `port` must point to a valid NUL-terminated string for the duration of the call.
 */
struct MbusGoSerialClient *mbus_go_serial_client_new_rtu(const char *port,
                                                         uint32_t baud_rate,
                                                         uint8_t data_bits,
                                                         uint8_t parity,
                                                         uint8_t stop_bits,
                                                         uint32_t response_timeout_ms);
#endif

#if (defined(MBUS_FEATURE_GO) && !defined(MBUS_TARGET_WASM32))
/**
 * Creates a new ASCII serial client.
 *
 * See [`mbus_go_serial_client_new_rtu`] for parameter semantics.
 *
 * # Safety
 *
 * `port` must point to a valid NUL-terminated string for the duration of the call.
 */
struct MbusGoSerialClient *mbus_go_serial_client_new_ascii(const char *port,
                                                           uint32_t baud_rate,
                                                           uint8_t data_bits,
                                                           uint8_t parity,
                                                           uint8_t stop_bits,
                                                           uint32_t response_timeout_ms);
#endif

#if (defined(MBUS_FEATURE_GO) && !defined(MBUS_TARGET_WASM32))
/**
 * Destroys the serial client and releases all associated resources.
 *
 * # Safety
 *
 * `handle` must be a valid pointer originally returned by one of the
 * `mbus_go_serial_client_new_*` constructors.  Calling this function more
 * than once with the same pointer is undefined behaviour.
 */
void mbus_go_serial_client_free(struct MbusGoSerialClient *handle);
#endif

#if (defined(MBUS_FEATURE_GO) && !defined(MBUS_TARGET_WASM32))
/**
 * Opens the underlying serial port.
 *
 * # Safety
 *
 * `handle` must be a valid client pointer.
 */
MbusGoStatus mbus_go_serial_client_connect(struct MbusGoSerialClient *handle);
#endif

#if (defined(MBUS_FEATURE_GO) && !defined(MBUS_TARGET_WASM32))
/**
 * Closes the underlying serial port.
 *
 * # Safety
 *
 * `handle` must be a valid client pointer.
 */
MbusGoStatus mbus_go_serial_client_disconnect(struct MbusGoSerialClient *handle);
#endif

#if (defined(MBUS_FEATURE_GO) && !defined(MBUS_TARGET_WASM32))
/**
 * Sets the per-request timeout in milliseconds (`0` = no timeout).
 *
 * # Safety
 *
 * `handle` must be a valid client pointer.
 */
void mbus_go_serial_client_set_request_timeout_ms(struct MbusGoSerialClient *handle,
                                                  uint64_t timeout_ms);
#endif

#if (defined(MBUS_FEATURE_GO) && !defined(MBUS_TARGET_WASM32))
/**
 * Reads `quantity` holding registers (FC03).
 *
 * # Safety
 *
 * `handle`, `out_buf`, and `out_count` must be valid non-null pointers.
 */
MbusGoStatus mbus_go_serial_client_read_holding_registers(struct MbusGoSerialClient *handle,
                                                          uint8_t unit_id,
                                                          uint16_t address,
                                                          uint16_t quantity,
                                                          uint16_t *out_buf,
                                                          uint16_t out_buf_len,
                                                          uint16_t *out_count);
#endif

#if (defined(MBUS_FEATURE_GO) && !defined(MBUS_TARGET_WASM32))
/**
 * Writes a single holding register (FC06).
 *
 * # Safety
 *
 * `handle` must be a valid client pointer.
 */
MbusGoStatus mbus_go_serial_client_write_single_register(struct MbusGoSerialClient *handle,
                                                         uint8_t unit_id,
                                                         uint16_t address,
                                                         uint16_t value,
                                                         uint16_t *out_address,
                                                         uint16_t *out_value);
#endif

#if (defined(MBUS_FEATURE_GO) && !defined(MBUS_TARGET_WASM32))
/**
 * Writes multiple holding registers (FC16).
 *
 * `values` is a slice of `quantity` big-endian register words.
 *
 * # Safety
 *
 * `handle` and `values` must be valid non-null pointers.
 */
MbusGoStatus mbus_go_serial_client_write_multiple_registers(struct MbusGoSerialClient *handle,
                                                            uint8_t unit_id,
                                                            uint16_t address,
                                                            const uint16_t *values,
                                                            uint16_t quantity,
                                                            uint16_t *out_address,
                                                            uint16_t *out_quantity);
#endif

#if (defined(MBUS_FEATURE_GO) && !defined(MBUS_TARGET_WASM32))
/**
 * Reads `quantity` input registers (FC04).
 *
 * # Safety
 *
 * `handle`, `out_buf`, and `out_count` must be valid non-null pointers.
 */
MbusGoStatus mbus_go_serial_client_read_input_registers(struct MbusGoSerialClient *handle,
                                                        uint8_t unit_id,
                                                        uint16_t address,
                                                        uint16_t quantity,
                                                        uint16_t *out_buf,
                                                        uint16_t out_buf_len,
                                                        uint16_t *out_count);
#endif

#if (defined(MBUS_FEATURE_GO) && !defined(MBUS_TARGET_WASM32))
/**
 * Applies an AND/OR bitmask to a holding register (FC22).
 *
 * # Safety
 *
 * `handle` must be a valid client pointer.
 */
MbusGoStatus mbus_go_serial_client_mask_write_register(struct MbusGoSerialClient *handle,
                                                       uint8_t unit_id,
                                                       uint16_t address,
                                                       uint16_t and_mask,
                                                       uint16_t or_mask);
#endif

#if (defined(MBUS_FEATURE_GO) && !defined(MBUS_TARGET_WASM32))
/**
 * Reads and simultaneously writes multiple holding registers (FC23).
 *
 * # Safety
 *
 * `handle`, `write_values`, `out_buf`, and `out_count` must be valid non-null pointers.
 */
MbusGoStatus mbus_go_serial_client_read_write_multiple_registers(struct MbusGoSerialClient *handle,
                                                                 uint8_t unit_id,
                                                                 uint16_t read_address,
                                                                 uint16_t read_quantity,
                                                                 uint16_t write_address,
                                                                 const uint16_t *write_values,
                                                                 uint16_t write_quantity,
                                                                 uint16_t *out_buf,
                                                                 uint16_t out_buf_len,
                                                                 uint16_t *out_count);
#endif

#if ((defined(MBUS_FEATURE_GO) && !defined(MBUS_TARGET_WASM32)) && defined(MBUS_FEATURE_COILS))
/**
 * Reads `quantity` coils (FC01).
 *
 * # Safety
 *
 * `handle`, `out_buf`, and `out_count` must be valid non-null pointers.
 */
MbusGoStatus mbus_go_serial_client_read_coils(struct MbusGoSerialClient *handle,
                                              uint8_t unit_id,
                                              uint16_t address,
                                              uint16_t quantity,
                                              uint8_t *out_buf,
                                              uint16_t out_buf_len,
                                              uint16_t *out_count);
#endif

#if ((defined(MBUS_FEATURE_GO) && !defined(MBUS_TARGET_WASM32)) && defined(MBUS_FEATURE_COILS))
/**
 * Writes a single coil (FC05).
 *
 * # Safety
 *
 * `handle` must be a valid client pointer.
 */
MbusGoStatus mbus_go_serial_client_write_single_coil(struct MbusGoSerialClient *handle,
                                                     uint8_t unit_id,
                                                     uint16_t address,
                                                     uint8_t value,
                                                     uint16_t *out_address,
                                                     uint8_t *out_value);
#endif

#if ((defined(MBUS_FEATURE_GO) && !defined(MBUS_TARGET_WASM32)) && defined(MBUS_FEATURE_COILS))
/**
 * Writes multiple coils (FC15).
 *
 * # Safety
 *
 * `handle` and `packed_coils` must be valid non-null pointers.
 */
MbusGoStatus mbus_go_serial_client_write_multiple_coils(struct MbusGoSerialClient *handle,
                                                        uint8_t unit_id,
                                                        uint16_t address,
                                                        const uint8_t *packed_coils,
                                                        uint16_t byte_count,
                                                        uint16_t coil_count,
                                                        uint16_t *out_address,
                                                        uint16_t *out_quantity);
#endif

#if ((defined(MBUS_FEATURE_GO) && !defined(MBUS_TARGET_WASM32)) && defined(MBUS_FEATURE_DISCRETE_INPUTS))
/**
 * Reads `quantity` discrete inputs (FC02).
 *
 * # Safety
 *
 * `handle`, `out_buf`, and `out_count` must be valid non-null pointers.
 */
MbusGoStatus mbus_go_serial_client_read_discrete_inputs(struct MbusGoSerialClient *handle,
                                                        uint8_t unit_id,
                                                        uint16_t address,
                                                        uint16_t quantity,
                                                        uint8_t *out_buf,
                                                        uint16_t out_buf_len,
                                                        uint16_t *out_count);
#endif

#if ((defined(MBUS_FEATURE_GO) && !defined(MBUS_TARGET_WASM32)) && defined(MBUS_FEATURE_FIFO))
/**
 * Reads the FIFO queue (FC24).
 *
 * # Safety
 *
 * `handle`, `out_buf`, and `out_count` must be valid non-null pointers.
 */
MbusGoStatus mbus_go_serial_client_read_fifo_queue(struct MbusGoSerialClient *handle,
                                                   uint8_t unit_id,
                                                   uint16_t address,
                                                   uint16_t *out_buf,
                                                   uint16_t out_buf_len,
                                                   uint16_t *out_count);
#endif

#if ((defined(MBUS_FEATURE_GO) && !defined(MBUS_TARGET_WASM32)) && defined(MBUS_FEATURE_DIAGNOSTICS))
/**
 * Reads the device exception status (FC07).
 *
 * # Safety
 *
 * `handle` and `out_status` must be valid non-null pointers.
 */
MbusGoStatus mbus_go_serial_client_read_exception_status(struct MbusGoSerialClient *handle,
                                                         uint8_t unit_id,
                                                         uint8_t *out_status);
#endif

#if ((defined(MBUS_FEATURE_GO) && !defined(MBUS_TARGET_WASM32)) && defined(MBUS_FEATURE_DIAGNOSTICS))
/**
 * Reads the communication event counter (FC11).
 *
 * # Safety
 *
 * `handle`, `out_status`, and `out_event_count` must be valid non-null pointers.
 */
MbusGoStatus mbus_go_serial_client_get_comm_event_counter(struct MbusGoSerialClient *handle,
                                                          uint8_t unit_id,
                                                          uint16_t *out_status,
                                                          uint16_t *out_event_count);
#endif

#if ((defined(MBUS_FEATURE_GO) && !defined(MBUS_TARGET_WASM32)) && defined(MBUS_FEATURE_DIAGNOSTICS))
/**
 * Reads the communication event log (FC12).
 *
 * # Safety
 *
 * `handle`, `out_buf`, and `out_count` must be valid non-null pointers.
 */
MbusGoStatus mbus_go_serial_client_get_comm_event_log(struct MbusGoSerialClient *handle,
                                                      uint8_t unit_id,
                                                      uint8_t *out_buf,
                                                      uint16_t out_buf_len,
                                                      uint16_t *out_count);
#endif

#if ((defined(MBUS_FEATURE_GO) && !defined(MBUS_TARGET_WASM32)) && defined(MBUS_FEATURE_DIAGNOSTICS))
/**
 * Reports the server identifier (FC17).
 *
 * # Safety
 *
 * `handle`, `out_buf`, and `out_count` must be valid non-null pointers.
 */
MbusGoStatus mbus_go_serial_client_report_server_id(struct MbusGoSerialClient *handle,
                                                    uint8_t unit_id,
                                                    uint8_t *out_buf,
                                                    uint16_t out_buf_len,
                                                    uint16_t *out_count);
#endif

#if ((defined(MBUS_FEATURE_GO) && !defined(MBUS_TARGET_WASM32)) && defined(MBUS_FEATURE_DIAGNOSTICS))
/**
 * Sends a Diagnostics (FC08) request.
 *
 * # Safety
 *
 * `handle`, `out_sub_function`, `out_buf`, and `out_count` must be valid non-null
 * pointers. If `data_in_count > 0`, `data_in` must point to at least `data_in_count`
 * valid `u16` words.
 */
MbusGoStatus mbus_go_serial_client_diagnostics(struct MbusGoSerialClient *handle,
                                               uint8_t unit_id,
                                               uint16_t sub_function,
                                               const uint16_t *data_in,
                                               uint16_t data_in_count,
                                               uint16_t *out_sub_function,
                                               uint16_t *out_buf,
                                               uint16_t out_buf_len,
                                               uint16_t *out_count);
#endif

#if ((defined(MBUS_FEATURE_GO) && !defined(MBUS_TARGET_WASM32)) && defined(MBUS_FEATURE_FILE_RECORD))
/**
 * Reads one or more file records (FC14) over a serial connection.
 *
 * # Safety
 *
 * `handle`, `sub_reqs`, `out_buf`, and `out_count` must be valid non-null pointers.
 * `sub_reqs` must be valid for `sub_req_count` items.
 */
MbusGoStatus mbus_go_serial_client_read_file_record(struct MbusGoSerialClient *handle,
                                                    uint8_t unit_id,
                                                    const struct MbusGoSerialSubRequest *sub_reqs,
                                                    uint16_t sub_req_count,
                                                    uint16_t *out_buf,
                                                    uint16_t out_buf_len,
                                                    uint16_t *out_count);
#endif

#if ((defined(MBUS_FEATURE_GO) && !defined(MBUS_TARGET_WASM32)) && defined(MBUS_FEATURE_FILE_RECORD))
/**
 * Writes one or more file records (FC15) over a serial connection.
 *
 * # Safety
 *
 * `handle` and `sub_reqs` must be valid non-null pointers. `sub_reqs` must be valid
 * for `sub_req_count` items. For each item, `data` must be valid for `data_len` words.
 */
MbusGoStatus mbus_go_serial_client_write_file_record(struct MbusGoSerialClient *handle,
                                                     uint8_t unit_id,
                                                     const struct MbusGoSerialSubRequest *sub_reqs,
                                                     uint16_t sub_req_count);
#endif

#if (defined(MBUS_FEATURE_GO) && !defined(MBUS_TARGET_WASM32))
/**
 * Creates a new async TCP client targeting `host:port`.
 *
 * `host` must be a NUL-terminated UTF-8 string.  The returned pointer
 * must eventually be released with [`mbus_go_tcp_client_free`].  Returns
 * `null` if `host` is null, not valid UTF-8, or the underlying constructor
 * fails (for example because no Tokio runtime could be started).
 *
 * # Safety
 *
 * `host` must point to a valid NUL-terminated string for the duration of
 * this call.
 */
struct MbusGoTcpClient *mbus_go_tcp_client_new(const char *host, uint16_t port);
#endif

#if (defined(MBUS_FEATURE_GO) && !defined(MBUS_TARGET_WASM32))
/**
 * Releases an `MbusGoTcpClient` previously returned from
 * [`mbus_go_tcp_client_new`].
 *
 * Drops the underlying `AsyncTcpClient`, which signals its background
 * Tokio task to exit.  No-op if `handle` is null.  Safe to call exactly
 * once per handle.
 *
 * # Safety
 *
 * `handle` must be either null or a pointer previously returned from
 * [`mbus_go_tcp_client_new`] that has not already been freed.
 */
void mbus_go_tcp_client_free(struct MbusGoTcpClient *handle);
#endif

#if (defined(MBUS_FEATURE_GO) && !defined(MBUS_TARGET_WASM32))
/**
 * Establishes the TCP transport connection.
 *
 * Blocks the calling thread until the connection completes or fails.
 *
 * # Safety
 *
 * `handle` must be a non-null pointer previously returned from
 * [`mbus_go_tcp_client_new`] and still alive.
 */
MbusGoStatus mbus_go_tcp_client_connect(struct MbusGoTcpClient *handle);
#endif

#if (defined(MBUS_FEATURE_GO) && !defined(MBUS_TARGET_WASM32))
/**
 * Closes the TCP transport gracefully.
 *
 * Drains any in-flight or queued requests with `ConnectionClosed`.  After
 * this call the client can be reconnected with
 * [`mbus_go_tcp_client_connect`].
 *
 * # Safety
 *
 * See [`mbus_go_tcp_client_connect`].
 */
MbusGoStatus mbus_go_tcp_client_disconnect(struct MbusGoTcpClient *handle);
#endif

#if (defined(MBUS_FEATURE_GO) && !defined(MBUS_TARGET_WASM32))
/**
 * Sets a per-request timeout in milliseconds; `0` disables the timeout.
 *
 * # Safety
 *
 * See [`mbus_go_tcp_client_connect`].
 */
MbusGoStatus mbus_go_tcp_client_set_request_timeout_ms(struct MbusGoTcpClient *handle,
                                                       uint64_t timeout_ms);
#endif

#if (defined(MBUS_FEATURE_GO) && !defined(MBUS_TARGET_WASM32))
/**
 * Returns `1` when there are requests in flight awaiting a response, `0`
 * otherwise; returns `0` when `handle` is null.
 *
 * # Safety
 *
 * See [`mbus_go_tcp_client_connect`].
 */
uint8_t mbus_go_tcp_client_has_pending_requests(struct MbusGoTcpClient *handle);
#endif

#if (defined(MBUS_FEATURE_GO) && !defined(MBUS_TARGET_WASM32))
/**
 * Reads `quantity` holding registers (FC03) starting at `address` from
 * the given `unit_id` and copies them, in declaration order, into the
 * caller-supplied `out_buf` of length `out_buf_len` (in `u16` elements).
 *
 * On success writes the number of registers actually read into
 * `out_count` (which equals `quantity`) and returns `MbusOk`.
 *
 * Returns `MbusErrBufferTooSmall` if `out_buf_len < quantity`.
 *
 * # Safety
 *
 * * `handle` must be a valid client pointer.
 * * `out_buf` must point to writable storage for at least `out_buf_len`
 *   `u16` values.
 * * `out_count` must point to a writable `u16`.
 */
MbusGoStatus mbus_go_tcp_client_read_holding_registers(struct MbusGoTcpClient *handle,
                                                       uint8_t unit_id,
                                                       uint16_t address,
                                                       uint16_t quantity,
                                                       uint16_t *out_buf,
                                                       uint16_t out_buf_len,
                                                       uint16_t *out_count);
#endif

#if (defined(MBUS_FEATURE_GO) && !defined(MBUS_TARGET_WASM32))
/**
 * Writes a single holding register (FC06).
 *
 * On success, writes the echoed `(address, value)` to `out_address` and
 * `out_value` if those pointers are non-null.
 *
 * # Safety
 *
 * * `handle` must be a valid client pointer.
 * * `out_address` and `out_value` may be null; if non-null they must
 *   point to writable `u16` storage.
 */
MbusGoStatus mbus_go_tcp_client_write_single_register(struct MbusGoTcpClient *handle,
                                                      uint8_t unit_id,
                                                      uint16_t address,
                                                      uint16_t value,
                                                      uint16_t *out_address,
                                                      uint16_t *out_value);
#endif

#if (defined(MBUS_FEATURE_GO) && !defined(MBUS_TARGET_WASM32))
/**
 * Writes `quantity` holding registers (FC16) starting at `address`.
 *
 * `values` must point to `quantity` `u16` values in declaration order.
 *
 * On success, writes the echoed `(starting_address, quantity)` to
 * `out_address` and `out_quantity` if non-null.
 *
 * # Safety
 *
 * * `handle` must be a valid client pointer.
 * * `values` must point to at least `quantity` readable `u16` values.
 * * `out_address` and `out_quantity` may be null.
 */
MbusGoStatus mbus_go_tcp_client_write_multiple_registers(struct MbusGoTcpClient *handle,
                                                         uint8_t unit_id,
                                                         uint16_t address,
                                                         const uint16_t *values,
                                                         uint16_t quantity,
                                                         uint16_t *out_address,
                                                         uint16_t *out_quantity);
#endif

#if ((defined(MBUS_FEATURE_GO) && !defined(MBUS_TARGET_WASM32)) && defined(MBUS_FEATURE_COILS))
/**
 * Reads `quantity` coils (FC01) starting at `address` from `unit_id`.
 *
 * Writes bit-packed coil bytes (Modbus wire format, LSB-first) into `out_buf`.
 * `out_buf_len` is the buffer length in bytes; must be ≥ `ceil(quantity / 8)`.
 * On success writes the byte count to `out_count`.
 *
 * # Safety
 *
 * `handle`, `out_buf`, and `out_count` must be valid non-null pointers.
 */
MbusGoStatus mbus_go_tcp_client_read_coils(struct MbusGoTcpClient *handle,
                                           uint8_t unit_id,
                                           uint16_t address,
                                           uint16_t quantity,
                                           uint8_t *out_buf,
                                           uint16_t out_buf_len,
                                           uint16_t *out_count);
#endif

#if ((defined(MBUS_FEATURE_GO) && !defined(MBUS_TARGET_WASM32)) && defined(MBUS_FEATURE_COILS))
/**
 * Writes a single coil (FC05). `value != 0` means ON.
 *
 * On success, writes echoed address to `out_address` and value (`1`=ON, `0`=OFF)
 * to `out_value` if those pointers are non-null.
 *
 * # Safety
 *
 * `handle` must be a valid client pointer.
 */
MbusGoStatus mbus_go_tcp_client_write_single_coil(struct MbusGoTcpClient *handle,
                                                  uint8_t unit_id,
                                                  uint16_t address,
                                                  uint8_t value,
                                                  uint16_t *out_address,
                                                  uint8_t *out_value);
#endif

#if ((defined(MBUS_FEATURE_GO) && !defined(MBUS_TARGET_WASM32)) && defined(MBUS_FEATURE_COILS))
/**
 * Writes multiple coils (FC15) starting at `address`.
 *
 * `packed_coils` is Modbus bit-packed coil data (LSB-first). `byte_count` is
 * the number of bytes in `packed_coils`; `coil_count` is the number of coils.
 *
 * # Safety
 *
 * `handle` and `packed_coils` must be valid non-null pointers.
 */
MbusGoStatus mbus_go_tcp_client_write_multiple_coils(struct MbusGoTcpClient *handle,
                                                     uint8_t unit_id,
                                                     uint16_t address,
                                                     const uint8_t *packed_coils,
                                                     uint16_t byte_count,
                                                     uint16_t coil_count,
                                                     uint16_t *out_address,
                                                     uint16_t *out_quantity);
#endif

#if ((defined(MBUS_FEATURE_GO) && !defined(MBUS_TARGET_WASM32)) && defined(MBUS_FEATURE_DISCRETE_INPUTS))
/**
 * Reads `quantity` discrete inputs (FC02) starting at `address` from `unit_id`.
 *
 * Writes bit-packed bytes into `out_buf`. On success writes byte count to `out_count`.
 *
 * # Safety
 *
 * `handle`, `out_buf`, and `out_count` must be valid non-null pointers.
 */
MbusGoStatus mbus_go_tcp_client_read_discrete_inputs(struct MbusGoTcpClient *handle,
                                                     uint8_t unit_id,
                                                     uint16_t address,
                                                     uint16_t quantity,
                                                     uint8_t *out_buf,
                                                     uint16_t out_buf_len,
                                                     uint16_t *out_count);
#endif

#if (defined(MBUS_FEATURE_GO) && !defined(MBUS_TARGET_WASM32))
/**
 * Reads `quantity` input registers (FC04) starting at `address` from `unit_id`.
 *
 * # Safety
 *
 * `handle`, `out_buf`, and `out_count` must be valid non-null pointers.
 */
MbusGoStatus mbus_go_tcp_client_read_input_registers(struct MbusGoTcpClient *handle,
                                                     uint8_t unit_id,
                                                     uint16_t address,
                                                     uint16_t quantity,
                                                     uint16_t *out_buf,
                                                     uint16_t out_buf_len,
                                                     uint16_t *out_count);
#endif

#if (defined(MBUS_FEATURE_GO) && !defined(MBUS_TARGET_WASM32))
/**
 * Applies an AND/OR bitmask to a holding register (FC22).
 *
 * # Safety
 *
 * `handle` must be a valid client pointer.
 */
MbusGoStatus mbus_go_tcp_client_mask_write_register(struct MbusGoTcpClient *handle,
                                                    uint8_t unit_id,
                                                    uint16_t address,
                                                    uint16_t and_mask,
                                                    uint16_t or_mask);
#endif

#if (defined(MBUS_FEATURE_GO) && !defined(MBUS_TARGET_WASM32))
/**
 * Reads and simultaneously writes multiple holding registers (FC23).
 *
 * # Safety
 *
 * `handle`, `write_values`, `out_buf`, and `out_count` must be valid non-null pointers.
 */
MbusGoStatus mbus_go_tcp_client_read_write_multiple_registers(struct MbusGoTcpClient *handle,
                                                              uint8_t unit_id,
                                                              uint16_t read_address,
                                                              uint16_t read_quantity,
                                                              uint16_t write_address,
                                                              const uint16_t *write_values,
                                                              uint16_t write_quantity,
                                                              uint16_t *out_buf,
                                                              uint16_t out_buf_len,
                                                              uint16_t *out_count);
#endif

#if ((defined(MBUS_FEATURE_GO) && !defined(MBUS_TARGET_WASM32)) && defined(MBUS_FEATURE_FIFO))
/**
 * Reads the FIFO queue (FC24) at `address` from `unit_id`.
 *
 * # Safety
 *
 * `handle`, `out_buf`, and `out_count` must be valid non-null pointers.
 */
MbusGoStatus mbus_go_tcp_client_read_fifo_queue(struct MbusGoTcpClient *handle,
                                                uint8_t unit_id,
                                                uint16_t address,
                                                uint16_t *out_buf,
                                                uint16_t out_buf_len,
                                                uint16_t *out_count);
#endif

#if ((defined(MBUS_FEATURE_GO) && !defined(MBUS_TARGET_WASM32)) && defined(MBUS_FEATURE_DIAGNOSTICS))
/**
 * Reads the device exception status (FC07).
 *
 * # Safety
 *
 * `handle` and `out_status` must be valid non-null pointers.
 */
MbusGoStatus mbus_go_tcp_client_read_exception_status(struct MbusGoTcpClient *handle,
                                                      uint8_t unit_id,
                                                      uint8_t *out_status);
#endif

#if ((defined(MBUS_FEATURE_GO) && !defined(MBUS_TARGET_WASM32)) && defined(MBUS_FEATURE_DIAGNOSTICS))
/**
 * Reads the communication event counter (FC11).
 *
 * On success writes `status_word` and `event_count` to the respective out pointers.
 *
 * # Safety
 *
 * `handle`, `out_status`, and `out_event_count` must be valid non-null pointers.
 */
MbusGoStatus mbus_go_tcp_client_get_comm_event_counter(struct MbusGoTcpClient *handle,
                                                       uint8_t unit_id,
                                                       uint16_t *out_status,
                                                       uint16_t *out_event_count);
#endif

#if ((defined(MBUS_FEATURE_GO) && !defined(MBUS_TARGET_WASM32)) && defined(MBUS_FEATURE_DIAGNOSTICS))
/**
 * Reads the communication event log (FC12).
 *
 * Writes the raw event bytes into `out_buf`. On success writes byte count to `out_count`.
 *
 * # Safety
 *
 * `handle`, `out_buf`, and `out_count` must be valid non-null pointers.
 */
MbusGoStatus mbus_go_tcp_client_get_comm_event_log(struct MbusGoTcpClient *handle,
                                                   uint8_t unit_id,
                                                   uint8_t *out_buf,
                                                   uint16_t out_buf_len,
                                                   uint16_t *out_count);
#endif

#if ((defined(MBUS_FEATURE_GO) && !defined(MBUS_TARGET_WASM32)) && defined(MBUS_FEATURE_DIAGNOSTICS))
/**
 * Reports the server identifier (FC17).
 *
 * Writes the raw server ID bytes into `out_buf`. On success writes byte count to `out_count`.
 *
 * # Safety
 *
 * `handle`, `out_buf`, and `out_count` must be valid non-null pointers.
 */
MbusGoStatus mbus_go_tcp_client_report_server_id(struct MbusGoTcpClient *handle,
                                                 uint8_t unit_id,
                                                 uint8_t *out_buf,
                                                 uint16_t out_buf_len,
                                                 uint16_t *out_count);
#endif

#if ((defined(MBUS_FEATURE_GO) && !defined(MBUS_TARGET_WASM32)) && defined(MBUS_FEATURE_DIAGNOSTICS))
/**
 * Sends a Diagnostics (FC08) request.
 *
 * `sub_function` is the 16-bit sub-function code (see Modbus spec table).
 * `data_in` / `data_in_count` form the optional request data words (`null` / `0` for
 * sub-functions that carry no data such as *Return Query Data* with zero words).
 *
 * On success writes the echoed sub-function code to `out_sub_function` and the
 * echoed data words into `out_buf`, then sets `out_count` to the number of words written.
 *
 * # Safety
 *
 * `handle`, `out_sub_function`, `out_buf`, and `out_count` must be valid non-null pointers.
 * If `data_in_count > 0`, `data_in` must point to at least `data_in_count` valid `u16` words.
 */
MbusGoStatus mbus_go_tcp_client_diagnostics(struct MbusGoTcpClient *handle,
                                            uint8_t unit_id,
                                            uint16_t sub_function,
                                            const uint16_t *data_in,
                                            uint16_t data_in_count,
                                            uint16_t *out_sub_function,
                                            uint16_t *out_buf,
                                            uint16_t out_buf_len,
                                            uint16_t *out_count);
#endif

#if ((defined(MBUS_FEATURE_GO) && !defined(MBUS_TARGET_WASM32)) && defined(MBUS_FEATURE_FILE_RECORD))
/**
 * Reads one or more file records (FC14).
 *
 * `sub_reqs` must point to `sub_req_count` [`MbusGoSubRequest`] descriptors in read mode
 * (i.e. each `data` field is `null` and `data_len` is `0`).
 *
 * On success the register words for all sub-requests are written contiguously into
 * `out_buf` (in the order of the sub-requests), and `out_count` is set to the total
 * number of words written.  The caller is expected to know the `record_length` of each
 * sub-request so it can split the flat buffer into per-record slices.
 *
 * # Safety
 *
 * `handle`, `sub_reqs`, `out_buf`, and `out_count` must be valid non-null pointers.
 * `sub_reqs` must be valid for `sub_req_count` items.
 */
MbusGoStatus mbus_go_tcp_client_read_file_record(struct MbusGoTcpClient *handle,
                                                 uint8_t unit_id,
                                                 const struct MbusGoSubRequest *sub_reqs,
                                                 uint16_t sub_req_count,
                                                 uint16_t *out_buf,
                                                 uint16_t out_buf_len,
                                                 uint16_t *out_count);
#endif

#if ((defined(MBUS_FEATURE_GO) && !defined(MBUS_TARGET_WASM32)) && defined(MBUS_FEATURE_FILE_RECORD))
/**
 * Writes one or more file records (FC15).
 *
 * `sub_reqs` must point to `sub_req_count` [`MbusGoSubRequest`] descriptors in write mode
 * (i.e. each `data` field points to `data_len` valid `u16` words, and `record_length == data_len`).
 *
 * Returns `MbusOk` when all records have been written successfully.
 *
 * # Safety
 *
 * `handle` and `sub_reqs` must be valid non-null pointers.  `sub_reqs` must be valid for
 * `sub_req_count` items.  For each item, `data` must be valid for `data_len` words.
 */
MbusGoStatus mbus_go_tcp_client_write_file_record(struct MbusGoTcpClient *handle,
                                                  uint8_t unit_id,
                                                  const struct MbusGoSubRequest *sub_reqs,
                                                  uint16_t sub_req_count);
#endif

#if (defined(MBUS_FEATURE_GO) && !defined(MBUS_TARGET_WASM32))
uintptr_t mbus_go_tcp_client_handle_size(void);
#endif

#if (defined(MBUS_FEATURE_GO) && !defined(MBUS_TARGET_WASM32))
/**
 * Creates a new gateway handle.
 *
 * `host` is the NUL-terminated upstream bind address (e.g. `"0.0.0.0"`).
 * Returns `null` if `host` is null or not valid UTF-8.
 *
 * # Safety
 *
 * `host` must point to a valid NUL-terminated string.
 */
struct MbusGoTcpGateway *mbus_go_tcp_gateway_new(const char *host, uint16_t port);
#endif

#if (defined(MBUS_FEATURE_GO) && !defined(MBUS_TARGET_WASM32))
/**
 * Destroys the gateway handle.
 *
 * Call [`mbus_go_tcp_gateway_stop`] first if the gateway is running.
 *
 * # Safety
 *
 * `handle` must be a valid pointer returned by [`mbus_go_tcp_gateway_new`].
 */
void mbus_go_tcp_gateway_free(struct MbusGoTcpGateway *handle);
#endif

#if (defined(MBUS_FEATURE_GO) && !defined(MBUS_TARGET_WASM32))
/**
 * Adds a TCP downstream and returns its zero-based channel index.
 *
 * Returns `u32::MAX` if `host` is null.
 *
 * # Safety
 *
 * `handle` and `host` must be valid non-null pointers.
 */
uint32_t mbus_go_tcp_gateway_add_downstream(struct MbusGoTcpGateway *handle,
                                            const char *host,
                                            uint16_t port);
#endif

#if (defined(MBUS_FEATURE_GO) && !defined(MBUS_TARGET_WASM32))
/**
 * Routes a single unit ID to a downstream channel.
 *
 * Returns `MbusOk` on success, `MbusErrInvalidAddress` if `unit_id == 0` or
 * `channel >= registered_downstream_count`.
 *
 * # Safety
 *
 * `handle` must be a valid gateway pointer.
 */
MbusGoStatus mbus_go_tcp_gateway_add_unit_route(struct MbusGoTcpGateway *handle,
                                                uint8_t unit_id,
                                                uint32_t channel);
#endif

#if (defined(MBUS_FEATURE_GO) && !defined(MBUS_TARGET_WASM32))
/**
 * Routes an inclusive unit-ID range `[min, max]` to a downstream channel.
 *
 * Returns `MbusErrInvalidAddress` if `min == 0`, `min > max`, or
 * `channel >= registered_downstream_count`.
 *
 * # Safety
 *
 * `handle` must be a valid gateway pointer.
 */
MbusGoStatus mbus_go_tcp_gateway_add_range_route(struct MbusGoTcpGateway *handle,
                                                 uint8_t unit_min,
                                                 uint8_t unit_max,
                                                 uint32_t channel);
#endif

#if (defined(MBUS_FEATURE_GO) && !defined(MBUS_TARGET_WASM32))
/**
 * Starts the gateway on a new background OS thread.
 *
 * Connects all registered downstreams and begins forwarding requests.
 * Returns `MbusOk` immediately; the thread runs until
 * [`mbus_go_tcp_gateway_stop`] is called.
 *
 * Returns `MbusErrInvalidAddress` if no downstreams or no routes have been
 * registered.
 *
 * # Safety
 *
 * `handle` must be a valid gateway pointer.
 */
MbusGoStatus mbus_go_tcp_gateway_start(struct MbusGoTcpGateway *handle);
#endif

#if (defined(MBUS_FEATURE_GO) && !defined(MBUS_TARGET_WASM32))
/**
 * Signals the running gateway to stop.
 *
 * # Safety
 *
 * `handle` must be a valid gateway pointer.
 */
void mbus_go_tcp_gateway_stop(struct MbusGoTcpGateway *handle);
#endif

#if (defined(MBUS_FEATURE_GO) && !defined(MBUS_TARGET_WASM32))
/**
 * Creates a new TCP server handle.
 *
 * `host` is a NUL-terminated bind address (e.g. `"0.0.0.0"`).
 * `unit_id` is the Modbus unit ID to respond to.
 * `vtable` must remain valid for the lifetime of the server handle.
 *
 * Returns `null` if `host` or `vtable` is null, or if `unit_id` is invalid.
 *
 * # Safety
 *
 * * `host` must point to a valid NUL-terminated UTF-8 string.
 * * `vtable` must be non-null and the struct it points to must remain valid
 *   until [`mbus_go_tcp_server_free`] is called.
 */
struct MbusGoTcpServer *mbus_go_tcp_server_new(const char *host,
                                               uint16_t port,
                                               uint8_t unit_id,
                                               const MbusGoServerVtable *vtable);
#endif

#if (defined(MBUS_FEATURE_GO) && !defined(MBUS_TARGET_WASM32))
/**
 * Destroys the server handle.
 *
 * Does **not** stop a running server — call [`mbus_go_tcp_server_stop`]
 * first, wait for the background thread to exit, then call this.
 *
 * # Safety
 *
 * `handle` must be a valid pointer returned by [`mbus_go_tcp_server_new`].
 * Calling this more than once is undefined behaviour.
 */
void mbus_go_tcp_server_free(struct MbusGoTcpServer *handle);
#endif

#if (defined(MBUS_FEATURE_GO) && !defined(MBUS_TARGET_WASM32))
/**
 * Starts the server on a new background OS thread.
 *
 * The thread blocks on `serve_with_shutdown` until [`mbus_go_tcp_server_stop`]
 * is called or a fatal error occurs.  Returns immediately to the caller.
 *
 * # Safety
 *
 * `handle` must be a valid server pointer.
 */
MbusGoStatus mbus_go_tcp_server_start(struct MbusGoTcpServer *handle);
#endif

#if (defined(MBUS_FEATURE_GO) && !defined(MBUS_TARGET_WASM32))
/**
 * Signals the running server to stop.
 *
 * The background thread will finish in-flight sessions and then exit.
 *
 * # Safety
 *
 * `handle` must be a valid server pointer.
 */
void mbus_go_tcp_server_stop(struct MbusGoTcpServer *handle);
#endif

#if (defined(MBUS_FEATURE_GO) && !defined(MBUS_TARGET_WASM32))
/**
 * Returns a static C string describing `status`.
 *
 * Equivalent to [`crate::c::error::mbus_status_str`] but exported with a
 * `mbus_go_` prefix so that it appears in the Go-only header.
 *
 * The returned pointer is always valid (points to a static string literal).
 * The caller must NOT free it.
 */
const char *mbus_go_status_str(MbusGoStatus status);
#endif

#endif  /* MODBUS_RS_H */
