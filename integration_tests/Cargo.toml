[package]
name = "integration_tests"
version = "0.1.0"
edition = "2021"

[dependencies]
modbus-rs = { path = "../modbus-rs" }

heapless = "0.8.0"
anyhow = "1.0.86"
tokio = { version = "1", features = ["full"] }

[dev-dependencies]