# Generated by Cargo
# will have compiled files and executables
debug/
target/
facts/

# Remove Cargo.lock from gitignore if creating an executable, leave it for libraries
# More information here https://doc.rust-lang.org/cargo/guide/cargo-toml-vs-cargo-lock.html
Cargo.lock

# These are backup files generated by rustfmt
**/*.rs.bk

# MSVC Windows builds of rustc generate these, which store debugging information
*.pdb

/*.csv

# Project specific
.DS_Store

# Server app dispatcher — generated at build time by build.rs via MBUS_SERVER_APP_CONFIG.
# Lives in $OUT_DIR, not the source tree. This line is a safety net if it
# accidentally ends up in src/ during development.
mbus-ffi/src/c/server_gen/generated_server.rs
temp
**.temp.**
**.temp
**/build/**
**.db
**.class
**.dylib
**.debug.json
**.aiexclude
.vscode
.wasm-pack-cache
mbus-ffi/pkg
mbus-ffi/include
.cargo

# CMake project ignores
CMakeLists.txt.user
CMakeCache.txt
CMakeFiles
CMakeScripts
Testing
Makefile
cmake_install.cmake
install_manifest.txt
compile_commands.json
CTestTestfile.cmake
_deps
CMakeUserPresets.json
